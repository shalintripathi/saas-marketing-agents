# Maintenance Backlog

Prioritized queue for the [maintenance routine](ROUTINE.md). Each run pulls the top unblocked item. `P0` = correctness/health (do first). `P1` = high-leverage growth. `P2` = nice-to-have. Mark items `[x]` when done and note the date; keep this list honest.

## P0 — correctness & health (always first if present)
- [x] Fixed 2026-08-29: CI lint (`.github/workflows/lint-agents.yml`) covered only 11 of 17 agent-category dirs, silently skipping the 8 personas in `abm`/`comms`/`developer-marketing`/`events`/`growth`/`partnerships` (linted 67, not 75) with a stale "59 personas … 11 directories" comment. Expanded `dirs` to all 17 and corrected the comment; replayed the CI job locally under bash → 75 linted, 0 failed.
- [ ] (none open as of 2026-08-29 after the CI-coverage fix above; prior clean checkpoint 2026-08-23, after fixing six stale roster counts the 2026-08-23 sponsorship-buyer add (`71`→`72`) left in the live maintenance docs: `71`→`72` in this file's Notes badge-count instruction and the GitHub-Pages-catalog item, `59`→`72` in three present-tense roster references, and `71`→`72` in `SKILL_SCOUT.md`'s inventory step. All public surfaces — README badge, `AGENTS_INDEX.md`, `llms.txt`, `CITATION.cff`, both manifests, and the live GitHub About — were already correct at 72/19. Append-only history in `RUN_LOG.md`/`scout-ledger.md` left as dated records.)

## P1 — high-leverage
- [x] Add `brand-context.md` template + wire the CATALYST orchestrator to read it first, so agent output is tailored to a user's ICP/product/voice. — done 2026-07-21 (`templates/brand-context.md`; orchestrator Step 0).
- [x] Wire the 12 category skills to read `brand-context.md` too, so invoking a category skill directly (e.g. `/saas-marketing:content-marketing`) is as tailored as going through CATALYST. — done 2026-07-22 (Step 0 block in all 13 `SKILL.md` files).
- [x] Add a monthly ABM loop to `loops/` (account selection → tiered messaging → multi-channel sequencing → measurement). — done 2026-07-22 (`loops/monthly-abm-loop.md`; persistent account ledger carries state between runs).
- [x] Add a weekly competitive-intel loop to `loops/`. — done 2026-07-22 (`loops/weekly-competitive-intel-loop.md`; watchlist + snapshot carry state, severity triage gate, public-sources-only boundary).
- [x] Add the Step 0 brand-context block to `loops/weekly-content-engine-loop.md` (it predates the template; the monthly ABM loop has one). — done 2026-07-23 (all 3 loops now load brand context first; citation-specific fabrication guard + 2 checklist items).
- [ ] Expose the 76 personas as native Claude Code subagents under `plugins/saas-marketing/agents/` (convert frontmatter to subagent format; keep skill-referenced copies working). — scoped in [#1](https://github.com/shalintripathi/saas-marketing-agents/issues/1) (2026-07-23, when the roster was 59); blocked on two decisions in-thread (single source of truth vs. a 3rd hand-maintained copy; whether to rewrite all 72 descriptions to trigger-style) before implementation.
- [x] Add a demo GIF/asset for the README hero (orchestrator taking one brief and routing it). — done 2026-07-24 (`assets/catalyst-demo.svg`; animated SVG rather than a GIF, so it needs no screen recording, stays ~8 KB, and degrades to a readable static frame. ~12s reveal, not the 60–90s originally sketched — a hero asset that takes a minute to become legible works against the README).
- [x] Verify install flow end-to-end for Cursor, Copilot, Aider, Windsurf; correct any drift in `integrations/README.md`. — done 2026-07-23 (all 6 `--tool` paths exercised against a sandboxed `$HOME`; fixed the Copilot install target — `~/.copilot/agents/*.agent.md`, not `$HOME/.github/agents/*.md`; rewrote the guide against cited vendor docs and added a `Sources` section).

## P1 — distribution (pre-approved lists only; follow each list's exact rules)
> **Every item below is gated on community traction or a non-automatable process — verified against each list's own `CONTRIBUTING.md` on 2026-08-18. None is an "unblocked top item" an autonomous run should pull.** Current repo traction: **5 stars**. The near-term lever is genuine adoption (a human/organic milestone), not a submission — submitting under-traction risks an auto-close or a spam flag on a large curated list, and the repo's no-fabrication guardrail forbids padding an entry with "used by" claims we can't back. Do these only once the star/usage bars are actually met.
- [x] ~~Submit to `langgptai/awesome-claude-prompts`~~ — assessed 2026-07-21: it's a single-prompt library, not a repo-link list; not a clean fit, skipped.
- [x] Submit to `jmedia65/awesome-ai-marketing` — done 2026-07-21 ([PR #20](https://github.com/jmedia65/awesome-ai-marketing/pull/20)).
- [ ] Prepare + submit to `hesreallyhim/awesome-claude-code` (fill the ISSUE **form**, not a PR; human-written, no sales language). **Blocked on stars (verified 2026-08-18):** its `CONTRIBUTING.md` requires **≥100 stars** — *"Resources that fail these criteria will be closed automatically"* — and the maintainer explicitly asks projects to gain users *before* submitting, warning against list-as-promotion. At 5 stars this auto-closes; revisit past 100 stars once README + install are polished.
- [ ] Prepare + submit to `VoltAgent/awesome-agent-skills` (PR into Community → **Marketing** subcategory; description ≤10 words; PR title `Add skill: shalintripathi/saas-marketing-agents`). **Blocked on community usage (verified 2026-08-18):** its `CONTRIBUTING.md` requires *"real community usage… Brand new skills that were just created are not accepted,"* and reviewers "may decline skills that don't meet the quality bar." At 5 stars we can't honestly claim adopted-and-proven usage; revisit once genuine adoption exists (same traction gate as the `kyrolabs`/`sindresorhus` item below). Entry format captured above so an eligible future run can execute cleanly.
- [ ] Submit the plugin to the Anthropic community marketplace (`anthropics/claude-plugins-community`) via the in-app form; run `claude plugin validate` first.
- [ ] Revisit `kyrolabs/awesome-agents` and `sindresorhus/awesome` once the repo has ≥40 stars and 30+ days of history.

## P1 — skill curation (from scout 2026-07-21; the daily scout works these one per run — see [scout-ledger.md](scout-ledger.md))
- [x] Enhance `paid-media-attribution-analyst` with a Bayesian-MMM measurement backbone — done 2026-07-21 (src: pymc-marketing, Apache-2.0, ideas-only).
- [x] Enhance `sales-discovery-coach` with a methodology taxonomy (MEDDPICC/SPIN/Challenger/Gap/Sandler/Value) — done 2026-07-21 (src: gtm-skills, gtmagents; MIT).
- [x] Enhance `seo/seo-ai-search-optimizer`: 0-100 passage-citability rubric + citation-regression "tests" + AI Share-of-Voice heatmap — done 2026-07-26 (new "Measuring Citability: Score, Regress, Map" section in both copies; rubric weights flagged directional; ideas-only, credited to Auriti-Labs/geo-optimizer-skill, AgricIDaniel/claude-seo, seranking/seo-skills; MIT).
- [x] Enhance `paid-media/paid-media-ppc-strategist`: verify-before-write mutation guardrail + negative-keyword pipeline + weighted audit score — done 2026-07-26 (new "Operating a Live Account" section in both copies: four-state control model with health graded separately from evidence coverage, a three-tier spend-change gate, and a recurring search-term → negative-keyword loop with double-classification and a cross-level conflict check. Ideas-only; src fourteenwm/ppc-ai-skills, AgriciDaniel/claude-ads, hyperfx-ai/marketing-skills, MIT; Google Ads limits cited).
- [x] Enhance `paid-media/paid-media-social-ads-specialist`: creative-fatigue rule + Pixel/CAPI Event-Match-Quality audit — done 2026-07-26 (new "Reading the Two Silent Signals" section in both copies: fatigue as a coordinated-decline pattern with directional B2B refresh triggers, and an EMQ audit — CAPI-alongside-Pixel, correct parameter hashing, `event_id` deduplication, EMQ ≥ 7 target — under a hashed/consent-gated/aggregates-only PII boundary. Ideas-only; src TheMattBerman/meta-ads-kit, MIT; EMQ/parameter/dedup facts cited to Meta developer docs, read 2026-07-26).
- [x] Enhance `product-marketing/pmm-positioning-strategist`: named frameworks (Dunford Obviously Awesome, Crossing the Chasm, JTBD, StoryBrand, Blue Ocean ERRC) + portfolio-before-messaging — done 2026-07-26 (new "The Positioning Toolkit: Named Frameworks and When to Reach for Each" section in both dual-located copies: five frameworks each with an explicit *when to reach for it* diagnosis, plus a portfolio-before-message discipline. Ideas-only; frameworks attributed to their authors; src wondelai/skills, realjaymes/marketingagentskills, MIT; no numeric claims).
- [x] Enhance `client-ops/ops-quality-assurance`: Four U's copy-scoring rubric + AI-tell / banned-word screen — done 2026-07-26 (new "Scoring Copy: The Four U's and the AI-Tell Screen" section in both dual-located copies: each U graded weak/adequate/strong with a diagnostic and a gate on Useful+Ultra-specific, plus a two-bucket AI-tell/banned-word screen framed as a flag-for-rewrite not an auto-reject or authorship verdict, with an explicit no-"percent-AI" rule. Four U's attributed to Michael Masterson/AWAI; screen idea ideas-only, src cgallic/kai-cmo-harness, sergebulaev/linkedin-skills, MIT; no detection-accuracy claims).
- [x] Enhance `email/email-automation-engineer` with a pre-send safety gate (blast-radius tiers, read-only-by-default scopes, 9-item pre-send checklist) — done 2026-07-25 (src: CosmoBlk/email-marketing-bible, thatrebeccarae/claude-marketing; MIT, ideas-only).
- [x] Enhance `analytics/analytics-marketing-ops-architect` with a **web-analytics instrumentation quality audit** (GA4 key-event config, custom dimensions, PII in event parameters, attribution/lookback settings, `(not set)` traffic diagnosis, UTM-to-channel-grouping alignment). The agent is thorough on CRM/MAP data quality and silent on the measurement layer feeding every other analytics agent (verified 2026-07-25). — done 2026-07-27 (new "Auditing the Web-Analytics Measurement Layer (GA4)" section in both dual-located copies: six read-only-by-default checks graded pass/needs-work/broken with *unknown never rounding to pass*, PII flagged fail-loud P0, attribution audited from the actual Admin setting rather than an assumed default. Ideas-only; the trigger skill cognyai/claude-code-marketing-skills `/ga4-audit` is paywalled and was NOT adopted — dimensions credited alongside SpillwaveSolutions, MIT; all limits/policy/behavior cited to Google's GA4 docs, read 2026-07-27).
- [x] Enhance `sales/sales-deal-strategist` with a **buyer-indecision loss mode** (JOLT: judge / offer a recommendation / limit exploration / take risk off the table), separating indecision from status-quo preference and adding a no-decision loss classification. — done 2026-07-28 (new "The Third Loss Mode" section in both dual-located copies + a `No-Decision Loss Mix` metric; gap verified by grep before writing — the concept was absent repo-wide. Ideas-only; framework attributed to Dixon & McKenna (*The JOLT Effect*, 2022), surfaced by `LeadMagic/gtm-skills` (MIT); stats cited to two sources read 2026-07-28 with the 84% figure flagged as inconsistently framed between them).
- [x] Enhance `sales/sales-deal-strategist` or `sales/sales-outbound-strategist` with **multi-thread orchestration mechanics** (thread coverage as a measurable state, not a checklist) and evaluate the **signal-play family** (funding / hiring / job-change / earnings / product-launch triggers) against our existing signal-based sequencing — both noted from `LeadMagic/gtm-skills` (MIT) on 2026-07-28. — done 2026-07-28 (multi-thread half only). Overlap verified first: the **signal-play family was found already covered** by `sales-outbound-strategist` (funding/hiring/job-change/launch across Critical Rule #1, "Buying Signal Indicators," and the T1/T2/T3 "Intent Signal Prioritization"; only earnings-call triggers absent, a narrow public-co ICP) → **not duplicated**. The multi-thread half had a real gap: the existing Multi-Threading Execution Plan is a checklist and the one metric measuring it ("Stakeholder Engagement Score = *number* of stakeholders") rewards headcount, the exact anti-pattern. Shipped a **"Thread Coverage as a State, Not a Count"** block (grade each decision-critical role Covered / Corroborated / Current) + a **`Buying-Committee Coverage`** success metric in both dual-located copies of `sales-deal-strategist`.
- [x] Enhance `sales/sales-discovery-coach` with **transparency selling** (Caponi, *The Transparency Sale*) — proactive flaw disclosure, ordering of negatives, imperfection-believability, early pricing legibility. Last un-actioned idea from the `LeadMagic/gtm-skills` mine (MIT), flagged for future runs on 2026-07-28. — done 2026-07-28 (new "Selling by Disclosure" section in both dual-located copies; gap verified — "transparency" was repo-wide only as pricing/reporting transparency, the methodology had zero coverage; placed on the Discovery Coach, not the twice-touched-today Deal Strategist, since it's orthogonal to the question-taxonomy. Ideas-only; Spiegel ratings figure flagged directional. The LeadMagic mine is now fully worked).
- [x] Enhance `seo/seo-ai-search-optimizer` with an **AI-crawler access audit** (training vs. retrieval vs. user-triggered agents; robots.txt → edge/WAF → render; per-agent log verification) — done 2026-07-30 (new "Access Before Citation" section + Critical Rule 9 in both dual-located copies; gap verified by grep first — `GPTBot` appeared nowhere repo-wide. Ideas-only; surfaced by `zubair-trabzada/geo-seo-claude` and `Auriti-Labs/geo-optimizer-skill` (MIT), whose own bot tiering was *not* adopted where it contradicts vendor docs; every agent behavior cited to OpenAI / Anthropic / Perplexity / Google / Cloudflare primary documentation, read 2026-07-30).
- [x] PROPOSAL — new agent `seo/seo-programmatic-strategist` for scaled template-page SEO (integration / comparison / vs / alternatives / glossary pages, index-bloat guardrails). Net-new persona; decide scope/overlap with content-blog-strategist & pmm-competitive first (src: coreyhaines31, kostja94, claude-seo; MIT). — scoped in [#2](https://github.com/shalintripathi/saas-marketing-agents/issues/2) (2026-07-27) with a verified overlap analysis + a "net-new agent vs. capability section" decision to settle in-thread before any code lands; blocked on that decision. **Two further inputs for whoever settles it, from the 2026-08-06 scout:** `index bloat` returns **zero** hits repo-wide and [coffeegrind123/seo-manager](https://github.com/coffeegrind123/seo-manager) (MIT) scores it as a first-class signal — index-bloat guardrails are the strongest argument that this is a persona rather than a section, since nothing else in the repo has a reason to own them; and [Thibaultbm/claude-seo-geo](https://github.com/Thibaultbm/claude-seo-geo) (MIT) ships a page-type matrix / section library / site-archetype set plus comparison, collection, product, and service-page skills — the closest thing surveyed to a template-page taxonomy, worth reading before drafting scope. — **done 2026-08-17** (shipped in the roster-growth commit `d13e2ee`; [#2](https://github.com/shalintripathi/saas-marketing-agents/issues/2) closed). The persona-vs-section call was settled in favour of a **net-new persona**: `seo/seo-programmatic-strategist` (Programmatic SEO Strategist) owns programmatic-subfolder indexation policy and pre-committed index-bloat pruning (Rule 8) — the guardrails nothing else had a reason to own — with the overlap seams drawn explicitly in Rule 10 (Content Optimizer owns hand-written pages, Keyword Researcher hands the pattern/entity set, Technical Auditor owns sitewide crawl + schema, Competitive Intelligence supplies the `/vs` + `/alternatives` facts). Both dual copies present; lint 2/2.
- [ ] PROPOSAL (needs legal validation) — `client-ops/ops-legal-compliance`: AI-disclosure readiness (EU AI Act Art. 50) + C2PA content-provenance posture (src: indranilbanerjee/digital-marketing-pro, cgallic/kai-cmo-harness; MIT).

## P2 — later
- [ ] Add a golden input/output example per agent.
- [ ] GitHub Pages catalog with copy-to-clipboard for all 76 agents.
- [ ] MCP tool recipes (GA4, GSC, HubSpot, Ahrefs) to move agents from advisory to executable. **Consider promoting to P1** — the 2026-07-25 scout found the market moving decisively this way (OpenClaudia's 34 skills wire straight into Resend/HubSpot/GA4 APIs), and the 2026-07-26 paid scout found the same independently (hyperfx-ai's 24 skills over 100+ integrations; AgriciDaniel/claude-ads' capability-gated adapters across 12 ad platforms). Two independent signals in two days. Any such work inherits both gates now in the repo: the pre-send gate in `email-automation-engineer` and the spend-change gate in `paid-media-ppc-strategist` — read-only scopes by default, writes opt-in per task, sends and spend changes never implied.
- [ ] Evaluation harness: rubrics + regression checks for agent output.
- [ ] De-duplicate agent sources (single source of truth; generate the browseable and installable copies).

## Notes
- Social launch posts live in the maintainer's private launch kit and are **posted by a human**, never automated.
- Keep the README badge counts (agents: 77, skills: 19) and the agent index in sync when agents are added.

## P2 — skill curation (from the [Skill Scout](SKILL_SCOUT.md); see [scout-ledger.md](scout-ledger.md))
- [x] Give `analytics/analytics-conversion-rate-optimizer` the **trust half of experiment discipline** — the checks that decide whether to believe a result that is already powered and significant. The agent had power (Rule 2: sample size, 95%) and nothing on validity; grep-verified absent (`sample ratio mismatch`/`SRM`/`optional stopping`/`novelty effect`/`primacy effect` = 0 repo-wide; `guardrail metric` only on the Twitter agent; `peeking` only on the paid strategist), and Rule 2's "reach 95% before declaring a winner" actively invited peeking. — done 2026-08-10 (new "Trust the Split Before the Winner" section + Critical Rule 9 + Experiment Trust Report deliverable + Rule 2 pointer + a rewritten success metric that removes the "60-70% of tests win" quota, both dual-located copies. **SRM** as a chi-squared pre-verdict gate (strict `p < 0.001`, discard-not-adjust on unexplained mismatch), the **B2B small-N inversion** (the SRM check is itself underpowered at low volume → inspect the mechanism directly), **peeking**/optional-stopping discipline, **generalized guardrails**, the **novelty/primacy** durability read, and a gated ship/investigate/extend/stop/revert verdict with *invalid, rerun* in front. Seams drawn to `paid-media-creative-strategist` (power for paid tests) and `analytics-performance-analyst` (observational anomalies). Ideas-only; surfaced by `ab-test-analysis` in [phuryn/pm-skills](https://github.com/phuryn/pm-skills) (MIT, 25k★); SRM facts cited to Kohavi/Tang/Xu 2020 + Fabijan et al. KDD 2019; the small-N inversion, discard-not-adjust rule, guardrail generalization, and seams are ours; lint 2/2, copies diff-identical.)
- [x] Give `email/email-lifecycle-architect` (or `email-deliverability-specialist`) a **frequency opt-down ladder** — weekly → monthly → pause → sunset as a structured alternative to a hard unsubscribe, with each preference choice mapped to the suppression/frequency rule the ESP must honor. Verified gap 2026-07-30: we mention preference centers in `email-lifecycle-architect` but have no opt-down ladder, and "sunset" appears repo-wide only in a *social* agent. Surfaced by `aaron-he-zhu/aaron-marketing-skills` (Apache-2.0) — ideas only; Apache notice requirements apply to any direct adaptation. — done 2026-07-30 (new "The Opt-Down Ladder" section in both dual-located copies of `email-lifecycle-architect`; four rungs each mapped to concrete ESP enforcement + a guardrail that the ladder never gates the real one-click unsubscribe, a pause ≠ legal opt-out. Placed on the Lifecycle Architect with the sunset rung co-owned by the deliverability specialist, not duplicated. Ideas-only; compliance facts cited to Google sender guidelines / FTC CAN-SPAM / GDPR Art. 7(3), read 2026-07-30).
- [x] Give `product-marketing/pmm-messaging-architect` a **message-consistency audit method**. Rule 6 already mandates a quarterly coherence audit across website, sales materials, product UI, email, case studies, ads, and content, and a `Message Consistency Score` metric grades it — but the rule *names the surfaces and stops*. Three things are missing: (a) a **method** for what to compare (the source's four axes — the category claimed, the primary value led with, the audience addressed, and the proof cited — read surface-against-surface rather than each surface judged alone); (b) **prioritization by visibility × stakes**, so the homepage/pricing-page mismatch outranks the stale one-pager; (c) the **post-repositioning straggler case**, where old messaging survives in low-traffic corners nobody re-read. Note this is distinct from the vocabulary diff shipped 2026-07-31, which compares our copy to *buyers' words*; this compares our copy to *itself*. Likely a section on the same agent rather than a new persona, but check the seam with `ops-quality-assurance` (which owns copy *scoring*) before writing. Surfaced 2026-07-31 by `message-consistency-check` in [`pmalliance/product-marketing-skills`](https://github.com/pmalliance/product-marketing-skills) (MIT) — ideas only. — done 2026-07-31 (new "Auditing Message Consistency: Surface Against Surface" section in both dual-located copies: the *pair not the page* as the unit of audit, the four comparison axes, visibility × stakes triage, and a post-repositioning straggler sweep + a Rule 6 pointer, a Message Consistency Audit deliverable, and an upgraded metric. Seam confirmed before writing — `ops-quality-assurance` scores each surface against the brand standard in isolation and never across surfaces, so this is a method for an audit the Messaging Architect already owns, not a new owner; kept distinct from the same-day vocabulary diff. Ideas-only; the four axes / triage / straggler sweep are our framing; lint 2/2, copies diff-identical).
- [x] Give `analytics/analytics-marketing-ops-architect` a **consent-layer measurement audit** — the six-check GA4 instrumentation audit shipped 2026-07-27 governs what happens to data after it arrives and never asks whether it arrived. Gap grep-verified 2026-08-01: `Consent Mode`, `modeled conversion`, and `tag governance` returned **zero** hits repo-wide. Surfaced by the paywalled `/gtm-audit` in [cognyai/claude-code-marketing-skills](https://github.com/cognyai/claude-code-marketing-skills) — dimension only, skill not evaluable or adopted. — done 2026-08-01 (new "Consent Before Collection" section + Critical Rule 9 + a `Consent Coverage Transparency` metric in both dual-located copies: basic-vs-advanced as the choice that decides whether refused traffic is estimable or gone, the B2B threshold trap, an observed/modeled/missing decomposition where *unknown never rounds to observed*, three failure modes, the advertising half, and a boundary handing lawfulness to `ops-legal-compliance`. All facts cited to Google primary docs read 2026-08-01; the B2B threshold conclusion labeled as our reasoning, not a published figure; lint 2/2, copies diff-identical).
- [x] Evaluate the **tag-container hygiene** half of the same `/gtm-audit` dimension list — duplicate GA4 tags, orphaned tags, trigger coverage, naming conventions, and a weekly diff of tags/triggers/variables that flags risky changes and **unpublished container drafts**. This is the mechanical sibling of the consent work shipped 2026-08-01 and was deliberately left out of it (one change per run, and the consent half is the interpretation discipline an advisory persona is actually for). Decide first whether container hygiene belongs on `analytics-marketing-ops-architect` at all or is closer to a web-implementation task outside this repo's scope. Source is paywalled — dimensions only, no content to adopt. — done 2026-08-02. **Scope decided: the mechanical half is out of scope**, drawn explicitly in-file — orphaned tags, naming conventions, weekly container diffs, and unpublished-draft governance are analytics-engineering / web-implementation work, and this is an advisory persona that reads the container's *effect on the numbers*, not one that runs the container. Kept only the one measurement-invalidating sliver that is dead-center in the agent's own thesis and was genuinely absent (grep-verified — check 1's "only there" covers wrong *placement*, not double *counting*): a **seventh GA4 check, "Tag integrity,"** on a duplicated tag double-counting, with the "a metric moved with no marketing cause → suspect the container" tell borrowed from the consent banner-change confound and an annotate-container-publishes requirement. Duplicate-tag behavior quoted from Google Analytics Help (answer/12329709, read 2026-08-02); dimension credited to the paywalled `/gtm-audit`, no content adopted; lint 2/2, copies diff-identical.
- [x] Re-read [indranilbanerjee/digital-marketing-pro](https://github.com/indranilbanerjee/digital-marketing-pro) (MIT) properly on a future ops run. It has grown from the small repo assessed 2026-07-21 to 671★ / 158 skills / 24 agents and was pushed 2026-08-01. Its analytics skills are already covered by our inventory (checked 2026-08-01), but two *architecture* ideas are adjacent to `templates/brand-context.md` and were not evaluated in depth: the **Two-Views Model** (an unbiased market view held alongside a client-validated view, with a decision matrix governing which parts get re-run after validation) and a **Living Project Instruction File** that propagates a correction to every downstream skill. Scope against our existing brand-context + state-ledger pattern before treating either as an action. Also still the source of the open `ops-legal-compliance` proposal above. — done 2026-08-03. Read both specs directly (`two-views-model.md`, `decision-matrix-rerun.md`, `living-instruction-file-spec.md`, `update-back-rule.md`). **Two-Views Model declined** — it presupposes their 12-Part per-engagement deliverable lifecycle (unbiased v1 docs → a Part 5 client-validation stop → selective v2 re-runs), scaffolding this persona library deliberately doesn't have; the portable "market-view-vs-client-view gap is signal" kernel already lives across `pmm-competitive-intelligence` + the weekly intel loop. **LIF cache/auto-update machinery declined** (the single-source-of-truth file already *is* `brand-context.md`), but its **Update-Back correction kernel adopted**: gap grep-verified (write-back/propagation zero hits repo-wide, no agent owned it), shipped a **Rule 6 + §14 "Corrections & learnings"** to both copies of `templates/brand-context.md` so a discovered correction (a withdrawn proof point, a revised metric) propagates via a `[CORRECT BRAND CONTEXT: …]` marker instead of drifting into one deliverable. Ideas-only; src MIT, credited. `ops-legal-compliance` proposal still open above.
- [x] Give `product-marketing/pmm-messaging-architect` a **method for Rule 5's messaging validation**. Rule 5 says to test messaging with sales teams, customer advisory boards, and prospect interviews before rollout and "never launch messaging you haven't validated" — then names the audiences and stops. No definition of what counts as a signal, no decision rule for declaring a variant the winner, no handling of the case a B2B account actually lives in (the test cannot be powered), and no propagation step once something wins. Three things from the source are worth taking: (a) a **five-pillar audit score** per asset — ICP alignment, differentiation, clarity, emotion, cross-channel consistency — which turns "does this messaging work" into a diagnosis of *which* part fails; (b) a **pre-test feasibility gate** asked before the test is designed (is the ICP actually targetable on this channel, can the channel produce enough qualified interactions per variant inside the window, does measurement reach past clicks) — the messaging sibling of the pre-registration discipline shipped 2026-07-30; (c) a **composite signal scorecard** declaring a winner on a majority of mixed quantitative *and* qualitative signals — reply rate, landing conversion, scroll depth, 5-second clarity, objection reduction, stated preference, and "echo moments" where a prospect repeats your phrasing back unprompted. (c) is the important one: `paid-media-creative-strategist` already establishes that an underpowered test cannot declare a winner, and a B2B messaging test is *almost always* underpowered — so qualitative signals are not a consolation prize, they are the instrument. **Decide the seam first:** the Creative Strategist owns statistical validity for paid tests, this would own message validation across channels, and the two must not contradict each other on what "a winner" means. Surfaced 2026-08-04 by `message-market-fit` in [`Fearofsnakes/pmm-skillset`](https://github.com/Fearofsnakes/pmm-skillset) (MIT) — ideas only. — done 2026-08-05 (new "Validating the Message: Diagnose, Gate, Then Judge on a Composite" section + Message Validation Plan & Scorecard deliverable + Message Validation Integrity metric + a Rule 5 pointer, both dual-located copies; gap grep-verified — all eight validation terms returned zero hits. **Seam decided in-file:** a *validated message* and a *statistically-significant winner* are different verdicts; the Creative Strategist's four-outcome verdict is deferred to for any powered paid test; the composite scorecard is that agent's own "decide by structured judgment, label it as judgment" rung made into a method for the underpowered case that is B2B's norm — so the two never contradict on "a winner." All three source ideas ((a) five-pillar, (b) feasibility gate, (c) composite scorecard) taken; the seam resolution, grading scheme, gate checks, and majority-rule/qualitative-outweighs guardrails are ours. Source verified live + MIT (2026-08-05); ideas-only; no figures asserted; lint 2/2, copies diff-identical).
- [x] SCOPE QUESTION — **DECIDE** (escalated 2026-08-04: four independent sightings across three runs) — we have **no pricing/packaging owner**. Verified 2026-07-31: pricing appears repo-wide only as *competitor* pricing intelligence (`pmm-competitive-intelligence`) and *deal-level* pricing (`sales-proposal-architect`, `sales-deal-strategist`); nothing owns value metric selection, tier design, packaging, or willingness-to-pay research. Two independent sources surfaced the seam in a single run (`firatcand/founder-skills`, `TerminalSkills/skills`), which is why it was recorded rather than dismissed — and the 2026-08-04 PMM run found the **third and fourth** (`taizen-ai/taizen-claude-plugins` `pricing-packaging`; `matteotitta/genesys-skills` `pricing-strategy` + `pricing-research`), two of them from collections that are explicitly B2B-SaaS-GTM shaped rather than founder-generalist. The 07-31 note said a third sighting would make this worth a real decision; it now has four, so this item is **awaiting a maintainer decision, not more evidence**. Neither new source is worth adopting *from* — the signal is the recurrence, not the content. **The prior question is scope, not content:** whether pricing strategy belongs in a *marketing* skill collection at all, or is a product/finance discipline we should deliberately decline. That is a maintainer call, not a scout call — decide the boundary before anyone drafts an agent. Neither source is worth adopting from (both are short checklists). **Fifth sighting 2026-08-05** (`krillinai/growth-skills` `pricing-and-packaging-strategy`, MIT, found on an *email/analytics/ops* run — i.e. it now recurs across disciplines, not just within GTM collections). Recorded for count-honesty only; the item still needs a decision, not more evidence. — **done 2026-08-17** (decided by shipping, in roster-growth commit `d13e2ee`). The boundary was resolved **in favour of scope**: pricing belongs in the collection, now owned by `product-marketing/pmm-pricing-packaging-strategist` (Pricing & Packaging Strategist). It owns exactly the seam this item flagged as unowned — value-metric selection, the tier ladder / packaging matrix, end-to-end willingness-to-pay research, and discount floors / deal-desk approval — while consuming competitor pricing from `pmm-competitive-intelligence` as an input (Rule 2) and leaving deal-level pricing to `sales-proposal-architect` / `sales-deal-strategist`. None of the five surveyed sources was adopted from; the signal was the recurrence, the agent is original. Both dual copies present; lint 2/2.
- [x] Give `seo/seo-content-optimizer` (or decide it belongs on `content/content-blog-strategist`) a **cannibalization *detection* method for an existing library**. We already prevent it prospectively — `content-blog-strategist` Rule 8 enforces one keyword → one page through taxonomy governance — and as of 2026-08-02 we resolve it reactively, since Consolidate is one of the four dispositions in the new decay triage. What nothing owns is finding it: cluster the existing library by normalized keyword / stem / semantic-intent overlap and by subset containment, then grade severity by *overlap type × ranking disparity* into merge / canonical / differentiate / no-action, so that "several of our URLs alternate on this query and none of them wins" becomes detectable rather than noticed by accident. The source's dual-mode shape is worth keeping (free local analysis of titles, H1/H2s, meta and opening paragraphs; optional paid SERP validation on top) since it degrades gracefully without an API budget. **Decide the owner first** — detection reads like an audit of the existing library (Content Optimizer) while the fix is often a taxonomy decision (Blog Strategist), and the seam matters more than the section. Surfaced 2026-08-02 by `blog-cannibalization` in [AgriciDaniel/claude-blog](https://github.com/AgriciDaniel/claude-blog) (MIT) — ideas only. — done 2026-08-02. **Owner decided: `seo-content-optimizer`, not the Blog Strategist** — detection is a standing audit of the existing library, and the Optimizer already owns the reactive fix (Consolidate); the Blog Strategist keeps prospective prevention (Rule 8). The sharp gap, verified before writing: the Consolidate disposition only fires when a *decline* puts a cluster on the decay triage, so cannibalization that never declined (pages that were never good — alternating on one query for years, none winning, none falling far enough to trip a threshold) is structurally invisible to it. Shipped a new "Finding Cannibalization the Decay Triage Never Sees" section (standing library audit; cluster by subset containment / same-primary-keyword / semantic-intent overlap; severity = overlap type × ranking disparity, alternation = the true signature and a stable winner = no-action; four-way disposition merge / canonical / differentiate / no-action that is often *not* removal; never-merge-on-lexical-overlap-alone guardrail; dual-mode degrading gracefully without SERP data) + a Cannibalization Audit deliverable + a Cannibalization coverage metric, in both dual-located copies (lint 2/2, diff-identical). Ideas-only; dual-mode shape credited to the source, the decline-blind framing / subset-containment tell / severity grid / disposition are ours.
- [x] Evaluate **consent/suppression as a durable shared record** (a consent registry other agents read) rather than state living inside one ESP — same source, same run. Likely folds into the existing `templates/brand-context.md` + state-ledger pattern rather than becoming a new agent; scope before building. — done 2026-08-04. **Scope decided against `brand-context.md` as the record's home:** a consent/suppression *record* is contact-level operational PII whose system of record is the CRM/CDP, not a shared brand-config file (which carries ICP/voice/positioning, never contact lists). The genuine gap was not a *place* to store it but the *discipline* that it is one cross-channel record — grep-verified absent (`cross-channel` near suppress/consent: zero; the only adjacent mention, `email-deliverability-specialist` line 140, framed cross-platform suppression sync as *"preventing duplicate sends,"* a hygiene concern on the wrong owner). **Owner decided: the Legal Compliance Officer** (the advisory consent owner), not the channel agents — shipped Critical Rule 9 + a "Consent Is One Record; Every Channel Reads It Before Contact" section + a Cross-Channel Suppression Coverage metric in both dual-located copies of `client-ops/ops-legal-compliance.md` (lint 2/2, diff-identical). The section names the most-missed channel (ad-platform custom/matched audiences built from customer lists = a CPRA "share" that decays on each refresh; the Social Ads Specialist builds these with no opt-out-before-upload step) and elevates the deliverability specialist's line-140 sync from hygiene to consent obligation, extended past email to the ad and sales channels. Ideas-only; GDPR Art. 21/7(3) and CCPA/CPRA cited to primary statute; service-provider-vs-third-party analysis flagged for counsel; no fabricated figures. The "registry other agents *read*" mechanism was deliberately **not** built as a repo artifact — that resolves to the CRM/CDP query above, not to a new shared file or a new agent, so no residual item remains.
- [x] **Decide the owner for a platform-CPL-vs-CRM-CPL reconciliation**, then write it. `paid-media-attribution-analyst` already owns platform→CRM reconciliation at the *object* level (Rule 2, a 95%-of-conversions-map metric, and the CRM Integration & Data Mapping deliverable), so the discipline is not absent — but the sharp edge it misses is that **platform-reported CPL and CRM-derived CPL for the same campaign routinely disagree**, and the move is to reconcile them per campaign and report *the gap itself* as the finding rather than quietly picking the more flattering number. Also worth adopting is the ordering rule, which is the genuinely transferable part: run the conversion-tracking audit **first**, because a waste verdict computed on top of broken measurement is a confident wrong answer — that sequencing now matters more since the delivery-vs-targeting audit shipped 2026-08-03 gives the Social Ads Specialist a second way to produce a confident number over bad data. **The prior question is the seam:** measurement reconciliation is the Attribution Analyst's, but the campaign-level CPL verdict is the Social Ads Specialist's, and the answer decides whether this is a section or a cross-reference. Surfaced 2026-08-03 by `cpl-vs-crm-reconciliation` / `conversion-tracking-audit` in [mardab96/linkedin-ads-claude-skills](https://github.com/mardab96/linkedin-ads-claude-skills) (MIT) — ideas only. — done 2026-08-03. **Owner decided: the Attribution Analyst (it certifies the number); the campaign scale/pause verdict stays with the channel specialist** → a section on the owner + a one-sentence cross-reference from the Social Ads Specialist, not duplicated content. Gap grep-verified — `CPL` appeared nowhere in the Attribution Analyst (it owned object-level *count* reconciliation, not the derived ratio), and the ordering rule was genuinely absent (PPC Rule 2 is *build tracking before launching*, a setup rule, not *audit tracking before the verdict*). Shipped a new "Two CPLs Disagree" section + Critical Rule 10 + a CPL reconciliation coverage metric in both dual-located copies of `paid-media-attribution-analyst`, and the cross-reference in both copies of `paid-media-social-ads-specialist` (lint 4/4, copies diff-identical). Ideas-only, MIT-credited; gap-as-finding / "unmeasured is the finding" / certify-vs-verdict seam are ours; **no divergence figure asserted**. The **CPL-verdict-ownership question one level up** (whether the campaign verdict itself needs a cross-agent rule) is left as its own open item elsewhere; this shipped only the measurement half.
- [x] **Re-check [olson-adam/ad-mastermind](https://github.com/olson-adam/ad-mastermind) (MIT) on the next paid run**, and if it has held up, take its **discrimination control** to `client-ops/ops-quality-assurance` — *not* to a paid agent. The idea: when blind-judging a set of concepts, seed a deliberately weak decoy into the set; a judge that fails to rank the decoy last has invalidated that entire scoring round as flattery rather than evaluation. That is a direct, testable answer to the failure mode our reference-class gate (shipped 2026-07-31) explicitly names but has no way to *detect* — "a generous scorer is useless." Deliberately not adopted on sight: the repo was 0★ and days old when surveyed 2026-08-03, and a scoring-validity mechanism is exactly the kind of thing that should be seen to survive contact before we teach it. Its second idea — live competitor-ad field scans against the public Meta and LinkedIn ad libraries — is public-source competitive creative intel that currently has no owner; scope it against `pmm-competitive-intelligence` before treating it as an action. — **done 2026-08-06** (discrimination-control half). Re-checked the source: still live + MIT, still 0★ but now carrying **documented field-scan and gauntlet runs that log where their own blinding failed** (`blinding_failed` in their scorecard) rather than hiding it — the "seen to survive contact" bar this note set, met by the honest limitation-reporting rather than by stars, which the repo's philosophy has never treated as evidence. Gap grep-verified — `decoy` / `negative anchor` / `discrimination control` / `blind judg*` all zero repo-wide. Shipped a new **"The Discrimination Control: Proving the Scorer Actually Discriminated"** section to both dual-located copies of `ops-quality-assurance`: seed a weak negative-anchor decoy into a blind-judged *set*, read its placement first, void the round if it isn't near the bottom; + an honest-decoy construction rule, a log-pass-or-fail discipline mirroring the source's own transparency, and a **decoy proves discrimination not quality** guardrail (ratifies the ranking, never the winner; set-scoring only). Placed wholly on this agent (owns copy scoring, no cross-agent seam). Ideas-only, credited in-file; the set-size boundary, honest-decoy rule, and proves-discrimination-not-quality guardrail are ours; no detection/scoring figures asserted; lint 2/2, copies diff-identical. **The second idea (competitor-ad field scans vs. `pmm-competitive-intelligence`) — done 2026-08-07.** Scope decided: a competitor's live advertising is an independent CI *source* Rule 1 omitted, so it belongs on `pmm-competitive-intelligence` (feeding its existing profiles/battle-cards/dashboard), not on a paid agent; `paid-media-creative-strategist` has zero competitor-ad coverage so no seam conflict, and the creative-*inspiration* use is handed to it by a one-line note rather than duplicated. Gap grep-verified (`ad library`/`transparency center`/`working material`/`creative velocity`/`phrase-frequency` = zero across `product-marketing/`,`paid-media/`,`plugins/`). Shipped **Critical Rule 9** + a **"Reading Competitors' Live Advertising"** section + a **Competitor Ad-Activity Scan** deliverable + an **Advertising Signal Coverage** metric in both dual-located copies, pointing at the three official free public libraries (LinkedIn Ad Library, Google Ads Transparency Center, Meta Ad Library — the source's paid Apify scraper deliberately not adopted), with a copyrighted-working-material/never-redistribute boundary, a presence-is-a-claim-not-a-result rule, an absence-is-unknown rule, and creative-volume-as-a-state. Ideas-only, MIT-credited; platform facts cited to primary sources read 2026-08-07; no competitor spend/impression/effectiveness figure asserted; lint 2/2, copies diff-identical. **The `olson-adam/ad-mastermind` item is now fully worked.**
- [x] **Decide who owns the instrumentation *contract*** — the tracking plan that precedes every measurement audit we already ship. Coverage today is real but downstream and scattered: `analytics-marketing-ops-architect` audits GA4 config (07-27), consent (08-01), and the field layer (07-30), and `analytics-performance-analyst` Rule 1 requires documented metric definitions — but nothing owns the spec that says which events exist, what each one *means*, which system is authoritative for it, how identity and deduplication resolve, what consent purpose it carries, and how it is **versioned** so that changing a trigger or an eligibility rule produces a visible series break instead of a silent one. Gap grep-verified 2026-08-05: `event taxonomy` and `event schema` return zero hits repo-wide, and "tracking plan" appears only as a one-line deliverable bullet in two playbooks and a handoff checklist. This matters more since 2026-08-05, because the anomaly section shipped that day *depends* on a maintained series-break register and currently asks the analyst to keep one without saying who authors it. Worth taking from the source: value-states modeled before events (`invite_created → invite_delivered → invite_accepted → collaborator_reached_value`), an authoritative source per fact, and the rule that a new optional property is backward-compatible while a changed trigger, entity, or eligibility is not. **The prior question is scope** — a full product-instrumentation contract is analytics-engineering work this repo has already declared out of scope once (container hygiene, 08-02), so the decision is whether the *marketing* slice (campaign, lifecycle, and conversion events) is a section on the Ops Architect or something larger. Surfaced by `tracking-plan` + `growth-metrics-design` in [krillinai/growth-skills](https://github.com/krillinai/growth-skills) (MIT) — ideas only. — **DONE 2026-08-18.** Scope question answered: the **marketing slice** (campaign response, form and lead capture, content and conversion events, lifecycle-stage transitions) shipped as a section on `analytics-marketing-ops-architect` — new **Critical Rule 10**, the **"The Instrumentation Contract: Deciding What an Event Means Before Anyone Counts It"** section, a **Marketing Instrumentation Contract** deliverable, and an **Instrumentation Contract Coverage** metric. The **product** slice stays with `growth-plg-activation-strategist` (already owns a Product Event Taxonomy & Activation Tracking Plan deliverable in more depth), and pipeline/warehouse/container work stays out of scope per the 2026-08-02 container-hygiene declination. Both krillinai ideas shipped — authoritative source per fact (extended into the four-counts-one-authority framing, with *crediting* routed to `paid-media-attribution-analyst`) and the backward-compatible-vs-breaking split (extended into the visible-break rule and wired to `analytics-performance-analyst`'s series-break register, whose first three instrumentation rows now have an upstream author). Three further MIT sources co-credited (haiqigeng, samikshapattanaik-cmyk, skohltfarber). Lint 2/2, copies diff-identical.
- [x] ~~Read the `references/data-input-integrity.md`, `references/audit-findings-discipline.md` and `references/bayesian-testing.md` files of [sidchaudhary/gtm-skills](https://github.com/sidchaudhary/gtm-skills) on a future **analytics/ops** run~~ — **worked 2026-08-21.** All three read in full; repo re-verified as still maintained (MIT, pushed 2026-08-19, not archived, still 0★ — read for ideas, nothing adopted on trust). `bayesian-testing.md` **shipped**: expected loss, practical equivalence as a terminal verdict, and the intended-vs-flat allocation point are now in `analytics-conversion-rate-optimizer` (see [RUN_LOG.md](RUN_LOG.md) 2026-08-21). `data-input-integrity.md` **dismissed as substantially covered** by `analytics-performance-analyst`'s comparability freeze, maturity section and absolute-floor rule — residual filed below. `audit-findings-discipline.md` **re-filed below** for an ops run.
- [x] Two residual checks from `sidchaudhary/gtm-skills` `references/data-input-integrity.md` (MIT), on a future **analytics/ops** run: **silent export truncation** (many tools cap an export at a row limit without saying so, and the analysis then confidently describes a truncated period) and **placeholder values parsed as numbers** (`0`, `1`, `9999`, `-1`, `N/A` standing in for missing data in a field a calculation depends on — a zero cost reads as infinite margin). Both are plausibly ours; the rest of that file's residual (currency mixing, tax/shipping inclusion, line-item-vs-order granularity) is e-commerce-shaped and was deliberately not taken. Likely home: `analytics-performance-analyst` or `ops-reporting-specialist`. Grep-verify first — `row limit`, `truncated`, and `placeholder` were unchecked as of 2026-08-21. — **done 2026-08-21.** Grep-verified both genuinely absent first: `truncat` existed only as the narrow GSC page-decay guard in `seo-content-optimizer` (identical row limits between two GSC pulls), not as a general reporting guard; every `placeholder` hit was about *copy* placeholders / hollow modifiers, and `9999`/`sentinel`/`magic number` returned nothing — the data-integrity concept was absent repo-wide. Shipped as a **"Read the export before you read the series"** subsection in both dual-located copies of `analytics/analytics-performance-analyst.md`, placed *upstream of* the existing comparability freeze (input integrity precedes comparability). Ideas-only, MIT-credited; the e-commerce residual deliberately still not taken; the reconciliation checks, the B2B-SaaS tells, and the placement are ours. Lint 2/2, copies diff-identical, no count change (71 / 19).
- [x] Read-and-decide on a future **ops/QA** run: `references/audit-findings-discipline.md` from [sidchaudhary/gtm-skills](https://github.com/sidchaudhary/gtm-skills) (MIT), read 2026-08-21 and held because it is a different discipline than that run's rotation. Three ideas worth weighing for `ops-quality-assurance` and `seo-technical-auditor`, both of which currently produce audits with no stated expiry: an audit carries a **re-audit trigger** (the event that makes it stale — a checkout change, a policy update — rather than a fixed interval); its **scope is stated specifically enough to reproduce** ("mobile Safari, guest checkout, one product, UK address", not "the checkout"); and findings are ranked on **severity × effort** in a four-cell grid (do now / plan / cheap wins / leave) rather than a flat severity list whose first three items all need engineering. Check against our existing four-state grading before adopting — the severity half may already be covered. — **done 2026-08-21.** Grep-decided per agent. `ops-quality-assurance` already carries a **staleness trigger** (its reference-data decay rule) and a **severity model with a resolution process** (critical/high/medium/low → tied action), so the severity half *is* covered there and adopting would gild. `seo-technical-auditor` had **none** of it (`re-audit`/`reaudit`/audit-expiry grep = 0), so the genuine single gap was there: shipped a **"An Audit Is a Dated Snapshot—Name What Voids It"** section (event-based void triggers drawn from the file's own migration/deploy sections; crawl date + reproducible scope + triggers at the top of the report) + a deliverable clause + an audit-shelf-life success metric, both copies, lint 2/2. The **severity × effort grid was deliberately not adopted** anywhere — `seo-technical-auditor` routes findings by disposition/owner and a generic priority grid over that is a maintainer's call, not a scout's; the `ops-quality-assurance` reproducible-scope-for-a-QA-pass residual is likewise low-value and left to a maintainer.
- [x] **Evaluate `growth-accounting`'s movement identity** for `analytics-performance-analyst` (or the Ops Architect): decompose period-over-period movement into new / resurrected / retained / churned / expanded / contracted as an **additive identity that must reconcile to the net change**, so "revenue is flat" resolves into the two large offsetting flows underneath it. Gap grep-verified 2026-08-05: `growth accounting` and `resurrect` return zero hits repo-wide, and our Cohort Analysis Report tracks cohort *progression* without ever closing the opening → inflow → outflow → closing loop. Natural follow-on to the decomposition discipline shipped 2026-08-05, which requires parent totals to reconcile but does not supply this particular identity. Two smaller notes from the same repo, recorded together: its `growth-data-quality-audit` overlaps our field layer / GA4 audit / four-state control heavily (its `verified/inferred/unavailable/not applicable` taxonomy is our own pattern relabeled) and is probably **not** worth a run; its `growth-decision-record` (what was decided, on what evidence, when to revisit) is adjacent to the `loops/` state ledgers and the correction-write-back discipline already shipped to `brand-context.md`, so check for duplication before treating it as an action. Source [krillinai/growth-skills](https://github.com/krillinai/growth-skills) (MIT) — ideas only. — done 2026-08-06. **Owner decided: `analytics-performance-analyst`, not the Ops Architect** — movement accounting is a *reporting* discipline on the stock metrics this agent already owns (Cohort Analysis Report, Executive Dashboard), not an instrumentation task. Shipped a new **"Movement Accounting: A Flat Number Is Two Flows"** section + **Critical Rule 10** + a **Period Movement Waterfall** deliverable (paired with the Cohort Analysis Report) + a **Movement Reconciliation** success metric, in both dual-located copies. The additive identity (Closing = Opening + New + Resurrected + Expansion − Contraction − Churned, reconciling exactly with the residual left visible) is the source's; **four layers are ours**: (a) the **count-vs-value distinction** — expansion and contraction exist only on value metrics, a logo is one logo whether it upgrades or not, so a count moves only on new/resurrected/churned; (b) the **five boundary decisions** (entity, identity, reactivation window, maturity, value) each framed as an *instrument change that reclassifies flows with no customer behavior*, cross-linked to the series-break discipline shipped 2026-08-05; (c) the **maturity bias on fresh-period outflow** — cancellations at term end, involuntary/dunning churn, and late-booked downgrades make the newest period *understate* outflow by construction, the movement-metric twin of the cohort-maturity argument; (d) the **voluntary-vs-involuntary churn split** and **contraction-as-leading-indicator** reads the net conceals. The SaaS quick ratio is named but asserted as a derived read, **not a target** — no ratio thresholds or churn benchmarks stated (a healthy profile is a property of a specific account). The two smaller notes (`growth-data-quality-audit`, `growth-decision-record`) were **not** actioned — both remain covered/adjacent as recorded; no residual item. Ideas-only; src MIT, credited in-file; lint 2/2, copies diff-identical, all three internal references resolve.
- [x] **Fix the stale Meta Audience Network recommendation** in `strategy/playbooks/phase-4-launch.md` (and its dual-located twin), which currently reads "Audience network: Include Messenger, Audience Network (broader reach)" with no B2B caveat. As of 2026-08-03 this directly contradicts what `paid-media-social-ads-specialist` now teaches about network placements diluting B2B delivery — the playbook tells a launching team to turn on the thing the specialist tells them to audit and usually switch off. Small, self-contained correctness fix; it is a *Meta* placement line so it needs its own verification against Meta's current documentation rather than reusing the LinkedIn citations. — done 2026-08-03. Both dual-located copies rewritten: the stale line (Meta's pre-rename "automatic placements" model, which "included" Audience Network as a broader-reach toggle) replaced with the current **Advantage+ placements** default (Facebook/Instagram/Messenger/WhatsApp/Audience Network), the Audience Network flagged as third-party consumer-app inventory (largely mobile games/entertainment) to audit against its own CPL and lead-quality bar rather than switch on for reach, and a pointer to the specialist's delivery-vs-targeting audit. Verified against Meta's own docs — [Advantage+ placements](https://www.facebook.com/business/ads/meta-advantage-plus/placements), [About Advantage+ placements](https://www.facebook.com/business/help/196554084569964), [How Meta Audience Network works](https://www.facebook.com/business/help/987564874649426) (read 2026-08-03) — not the LinkedIn citations; no waste percentage asserted (the figures circulating trace to agency blogs, not Meta). Copies diff-identical; it's a strategy playbook doc, not an agent file, so no lint schema applies.
- [x] Scope the **AEO ↔ Reddit seam**: our Field Guide already names Reddit as the highest-correlating off-page signal for B2B SaaS (≈6× G2) and `social-reddit-specialist` owns the channel, but *AEO-driven thread targeting* (finding the questions AI engines actually lift Reddit answers from, versus general community participation) is owned by neither. Decide whether it is a section on one of the two or a genuine seam. Surfaced 2026-07-30 by `onvoyage-ai/gtm-engineer-skills` (MIT) and its `reddit-opportunity-research` skill — ideas only. — done 2026-08-02. Decided: **a section on `social-reddit-specialist`, not a new persona** — it's a new prioritization lens on an activity the agent already performs (subreddit selection, high-intent-question identification), and the `seo-ai-search-optimizer` owns *your own site's* citability, so third-party-UGC execution mechanics would stretch its scope. Gap grep-verified first — the Reddit agent had zero mentions of AI engines/AEO/citations. Shipped a new "AEO-Aware Thread Targeting" section + Critical Rule 9 + an `AI-Answer Presence (AEO)` metric in both dual-located copies (lint 2/2, diff-identical). Ideas-only; figures directional/sourced in the playbook; the per-engine strategy handed back to `seo-ai-search-optimizer`.
- [x] Re-check `NEON-Rutger/B2B-revops-skills` `revops-data-governance` (field-prefix naming by owning team, data dictionary, deprecation policy) against `analytics-marketing-ops-architect` Critical Rule #3 — noted 2026-07-30 as adjacent but not yet compared in depth. — done 2026-07-30. Compared in depth: Rule #3 + the Data Dictionary deliverable already own field ownership, naming conventions, quality standards, and quarterly audits; the *data dictionary* half was **already covered, not duplicated**. Two disciplines were genuinely absent (grep-verified — `prefix` in zero agents, no field-deprecation lifecycle repo-wide): **field-prefix-by-owning-team** naming and a **field deprecation lifecycle**. Shipped both as a new "Governing the Field Layer" section in both dual-located copies of `analytics-marketing-ops-architect`. Ideas-only; src MIT, credited in-file; CRM specifics kept vendor-neutral.
- [x] Give `paid-media/paid-media-creative-strategist` a **statistical-power discipline** — decide before spend whether the account can detect the effect being tested; pre-register MDE and stopping rule; four outcome states with underpowered noise never rounding to a winner; alternatives when a test can't be powered. Verified gap 2026-07-30: `underpowered` and `minimum detectable` appeared **zero** times repo-wide against nine places prescribing 95% significance, and the agent's own success metrics ("40%+ lift at 95% confidence in 60 days" vs. "3-5 tests monthly") were mutually exclusive at B2B volumes. — done 2026-07-30 (new "Power Before Verdict: Testing at B2B Volumes" section + Critical Rule 9 in both dual-located copies; the two contradictory success metrics rewritten and a `Test validity rate` added. Ideas-only; pre-registration discipline from `AgriciDaniel/claude-ads` (MIT), variable-impact ordering from `borghei/Claude-Skills` (**MIT + Commons Clause** — restrictive/non-free, so ideas-only, license flagged in-file); all figures cited to LinkedIn's own A/B testing docs, read 2026-07-30. Meta's equivalent numbers deliberately omitted — its official help page returned no English body content this run and the figures circulating in secondary blogs weren't verifiable against a primary source).
- [x] Give `client-ops/ops-quality-assurance` a **scoring reference class**: the Four U's rubric shipped 2026-07-26 grades copy against generic best practice and never against *this brand's own historical performance*. Add the data gate — score against real historical performance data where it exists, against a named public benchmark where it doesn't, and against generic principles only when neither is available, **always labeling which of the three was used** — plus a staleness trigger that forces a refresh of the benchmark data. The operative discipline from the source: a generous scorer is useless, and every fix must point at a data point rather than an opinion. Surfaced 2026-07-30 by the `post-scorer` skill in `charlie947/social-media-skills` (MIT, 2.0k★) — ideas only; the rest of that repo is creator-economy shaped (Reels scripts, YouTube thumbnails, clickbait hook generators) and does not transfer to B2B. Likely reads from `templates/brand-context.md`; scope the data-source question before building. — done 2026-07-31 (new "The Reference Class: What You're Scoring Against" section in both dual-located copies: three-tier data gate — first-party history / named-dated benchmark / generic principles — with the tier label as the deliverable, a staleness trigger, and a never-manufacture-the-comparison guardrail. Gap grep-verified zero hits first. Ideas-only, credited in-file; no benchmark figures asserted).
- [x] **Decide the owner for AI-referral traffic measurement**, then write it. Our AEO stack now measures *access* (crawler reachability, 2026-07-30), *recognition* (entity, 2026-08-06), and *presence in answers* (share-of-voice heatmap, 2026-07-26) — and **nothing measures what arrives**, even though `seo-ai-search-optimizer`'s own Success Metrics already promise "attribute 5-10% of monthly qualified traffic to AI answer engine referrals." The sharp idea from the source: a large share of answer-engine traffic carries **no referrer at all** (native assistant apps, copy-pasted links, in-assistant browsers), so it files under Direct and the common read "we get almost no ChatGPT traffic" is routinely a measurement artifact rather than a finding — which is this repo's standing *unknown never rounds to observed* discipline landing in a new place. Worth taking: an explicit AI channel group matched on assistant source/referrer domains, UTM tagging only on links you actually control, conversion measured on the segment rather than sessions, and the honest statement that the referred share is a **floor**, not the total. Gap grep-verified 2026-08-06: `chatgpt.com`, `utm_source=chatgpt`, and `AI referral` return **zero** hits repo-wide; the two `referrer` hits are newsletter referral mechanics and all three `referral traffic` hits are `seo-link-building-strategist` on link quality. **The prior question is the seam:** channel groupings, custom channel groups, and the observed/modeled/missing decomposition are `analytics-marketing-ops-architect` ground (GA4 config audit 07-27, consent 08-01) while the AEO interpretation is the AI Search Optimizer's — so decide whether this is a section on the Ops Architect with a cross-reference, or a measurement subsection on the AEO agent, before writing. Surfaced 2026-08-06 by `ai-referral-analytics` in [jstanx/aeo-toolkit](https://github.com/jstanx/aeo-toolkit) (MIT) — ideas only. — done 2026-08-06. **Owner decided: a section on the AEO agent, construction handed to the Ops Architect** — the certify-vs-verdict seam from the 08-03 CPL reconciliation. The section belongs on `seo-ai-search-optimizer` because it completes that agent's own `reachable → recognized → quotable → arrived` funnel and backs its own unbacked *Citation traffic attribution* metric; the *interpretation* (floor-not-total, artifact-not-finding, judge-the-segment) is the AEO agent's, while the GA4 *construction* — the custom channel group, the referrer-domain match list, and the observed/modeled/missing decomposition — is `analytics-marketing-ops-architect`'s established craft and is deferred to it, with a one-sentence reciprocal pointer added to its GA4-audit check 6. Shipped a new **"Measuring Arrival: The Traffic AI Answers Actually Send"** section in both dual-located copies of the AEO agent (GA4 Direct definition quoted from Google, read 2026-08-06; controlled-links-only UTM rule; **no figure asserted** for the no-referrer share, which is unmeasured by nature). Ideas-only; src MIT, credited in-file; the floor framing, UTM rule, judge-the-segment discipline, and the seam are ours; lint 4/4, both copies of each agent diff-identical.
- [x] Read the B2B-relevant slice of [social-media-skills/skills](https://github.com/social-media-skills/skills) (14★, **106 skills**, MIT, pushed 2026-07-30) on a future **social** run. Surfaced by the 2026-08-07 paid/social scout and left on `watch`: the repo has **no paid coverage at all** and most of its roster is creator/DTC-shaped (Instagram, TikTok, Pinterest, Threads, thumbnail design, meme-and-culture) — the recurring audience mismatch — but four skills sit on ground our social agents hold thinly: `analytics-and-reporting` (a METER framework plus a `metrics-by-goal-and-report` reference), `experimentation-and-ab-testing`, `crisis-and-moderation`, and `linkedin-company-pages`. Relevant context: **`social-linkedin-strategist` is 85 lines and has never been enhanced**, and `dark post` returns zero hits repo-wide while crisis/moderation coverage exists only as passing mentions in `social-community-builder` and `social-twitter-strategist`. Also worth assessing on its own terms is the repo's **architecture**: every skill ships a `references/` set, several carry `evals/evals.json`, and each has a dated `<topic>-2026-reality.md` file that partitions *what is true now* from durable method — a different freshness convention from our inline dated citations, and a question worth a deliberate answer rather than a drift. Ideas only; grep-verify any gap before writing. — **PARTLY WORKED 2026-08-07** (the `linkedin-company-pages` slice). Read that skill in depth and shipped the **first-ever enhancement** to `social-linkedin-strategist` (the flagged never-touched 85-line agent): a first-hour distribution section covering the early-engagement window, the native *Notify Employees* tool (LinkedIn's once-per-24h cap + opt-out cost + relevance-model caveat), and link-placement-as-A/B-test, with the paid amplification path (Thought Leader Ads / dark posts) handed to `paid-media-social-ads-specialist`. Gap grep-verified (`Notify Employees`/`first-hour`/`golden hour`/`link in comment`/`dark post`/`Thought Leader Ad` = 0 hits repo-wide). Ideas-only, MIT-credited; Notify Employees facts cited to LinkedIn Help, first-hour/link effects flagged contested; the source's multiplier figures not adopted. **PARTLY WORKED 2026-08-08** (the `experimentation-and-ab-testing` slice). Read that skill: it teaches a TEST framework whose transferable spine is *controls-and-repetition instead of p-values* for organic social, which structurally can't reach significance. Gap grep-verified (`paired repetition`/`decision rule`/`holdout`/`underpowered`/`minimum detectable` = 0 hits across `social/`). **Owner decided: `social-twitter-strategist`** — the only B2B organic surface with the post volume to run 3–5 paired repetitions in 2–4 weeks, and its Rule 2 already commits to monthly variation testing with no method attached. Shipped Critical Rule 9 + a "Reading a Winner Off Organic Social" section + a Test Discipline metric; seam drawn to `paid-media-creative-strategist` (owns the powered paid case) so the two never contradict on "a winner." Ideas-only, MIT-credited; ~20% threshold flagged directional, no lift figure asserted; lint 2/2, copies diff-identical. **PARTLY WORKED 2026-08-09** (the `crisis-and-moderation` slice). The gap was real and grep-verified: `social-community-builder` owns *owned-community* crisis comms (breaches/outages/product issues), and the Twitter agent *named* crisis comms as a deliverable but Rule 8 covered only *steady-state* moderation — the *public-platform* pile-on/ratio was owned by nobody (`pile-on`/`backlash`/`negative virality`/`dogpile` = 0 hits across `social/`). **Owner decided: `social-twitter-strategist`** (X is the paradigmatic public-ratio surface, and it already held the deliverable). Shipped Critical Rule 10 + a "When Criticism Becomes a Pile-On: Managing a Public Ratio" section + a Crisis Response Discipline metric: triage-before-crisis, event-type→response mapping (correct misinformation with evidence, don't apologize for what didn't happen), acknowledge-fast/centralize, don't-scrub-legitimate-criticism (Streisand), pause the scheduled queue, agent-drafts-human-posts. Seam drawn to `social-community-builder` (owned space vs. public ratio). Ideas-only, MIT-credited; the acute-vs-steady-state split, the seam, the B2B framing, and the metric are ours; no figures asserted (the first-hour window reuses Rule 3's number); lint 2/2, copies diff-identical. **Still on watch:** the `<topic>-2026-reality.md` freshness-convention question, for future social runs. (`analytics-and-reporting` **assessed 2026-08-09 and declined as a non-gap** — the vanity-vs-actionable-metric discipline is already repo-wide, present in every `social-*` agent, and the goal→metric→report-structure discipline is squarely owned by `analytics-performance-analyst` + `ops-reporting-specialist`; the source is creator/DTC-shaped, so a social-specific reporting section would duplicate two existing owners. Not worth a future run.)
- [x] Give `product-marketing/pmm-launch-manager` a **method for Rule 8's post-launch retrospective**. Rule 8 ends "conduct post-launch retrospective within 2 weeks" and stops; `retrospective` appears repo-wide only as a scheduling bullet across the project-management agents and the phase playbooks, never as a technique. This got sharper on 2026-08-08, because the pre-mortem shipped that day hands the retrospective a specific job it did not previously have — re-open the risk register, record which predicted blocking risks materialized, and (the column that teaches) which real failures nobody named — so the seam is already drawn and only the method is missing. Worth taking from the source: a structure that separates *what happened* from *what we would decide differently*, and a rule that a retrospective without a written decision change is a debrief, not a retrospective. **Check the seam first** with `pm-marketing-ops-scrum-master` and `pm-campaign-coordinator`, which own retrospective *cadence* for campaigns — this would be launch-specific method on the launch owner, not a second scheduler. Surfaced 2026-08-08 by `retro` in [stefanoskarakasis/Product-Marketing-Skills](https://github.com/stefanoskarakasis/Product-Marketing-Skills) (MIT) — ideas only. — done 2026-08-08 (seam checked first — the two PM agents own retrospective *cadence* and *execution-versus-plan*, so this is launch-specific *method* on the launch owner, not a second scheduler; grep-verified `retrospective` was scheduling-only repo-wide). Shipped an updated **Rule 8** (points to the method, splits the two-week execution pass from the deferred sales-cycle market-outcome pass), an **"After It Ships: The Launch Retrospective"** section (two separated ledgers — *what happened* / *what we would decide differently*, a retrospective without a written decision change is a debrief; two-pass horizon; register-as-spine discharge of Rule 9's re-open; judge-the-decision-not-the-outcome), a **Post-Launch Retrospective & Decision Log** deliverable, and a **Retrospective Decision Yield** metric, in both dual-located copies. Ideas-only; two-ledger split + debrief rule credited to the source (verified live + MIT); the two-pass structure, register-as-spine, decision-vs-outcome discipline, and the seam are ours. No figures asserted; lint 2/2, copies diff-identical.
- [x] Evaluate **internal stakeholder mapping for a launch** — the approvers, blockers, and quiet vetoes inside your *own* company, as opposed to the buying committee. `sales-deal-strategist` maps the customer's committee (thread coverage as a state, shipped 2026-07-28) and `pmm-launch-manager` Rules 3–4 govern function sign-off and dependencies, but nothing models internal political risk, which is a named failure category in every pre-mortem taxonomy surveyed. Check overlap with `pm-campaign-coordinator` and `pm-client-success-manager`'s risk register before treating this as an action; it may well be covered enough. Surfaced 2026-08-08 by `stakeholder-maps` in [stefanoskarakasis/Product-Marketing-Skills](https://github.com/stefanoskarakasis/Product-Marketing-Skills) (MIT) — ideas only. — **done 2026-08-09**. The overlap check cleared all three: `pm-campaign-coordinator`'s stakeholder map is a *communications* plan (role, frequency, method, escalation), `pm-client-success-manager`'s register is *agency↔client* relationship risk, and `sales-deal-strategist` maps the *customer's* committee — none reads an internal stakeholder's stance toward the launch. Landed on `pmm-launch-manager` as a pre-mortem failure family rather than a standalone exercise (routing-table row + owning passage + deliverable extension + **Internal Alignment Read** metric), since the pre-mortem is already the moment designed to make private doubt sayable.
- [x] Read [rampstackco/claude-skills](https://github.com/rampstackco/claude-skills) (524★, MIT, ~100 skills, pushed 2026-08-10) properly on a future **content/SEO** run. Surfaced 2026-08-10 and left on `watch`: it is the best-organized SEO collection surveyed to date (16 SEO skills, each with a `references/` set, plus a `SKILLS.lock`, CI linting, and a `SKILL_AUTHORING.md`), and its `seo-technical` skill was read in full this run — its six-layer stack is well-built but its Indexability layer covers only *the signals you send*, so it corroborated rather than supplied the gap that shipped. Three things are worth a deliberate look, each against an agent of ours that is thin and never enhanced: (a) ~~its `references/migration-checklist.md` — **migrations are the single highest-risk technical SEO event and `seo-technical-auditor` says nothing about them**~~ — **done 2026-08-11**; (b) its `seo-traffic-diagnosis` hypothesis-before-fix discipline, adjacent to but distinct from `seo-content-optimizer`'s impostor check, which is page-level rather than site-level; ~~(c) its `content-refresh-system` + `pillar-content-architecture` pair against `content-blog-strategist` (82 lines, **never enhanced**, and one of seven content agents in that state)~~ — **done 2026-08-10** (as an adjacent-gap ship; the two named ideas resolved as non-gaps — see below). Ideas only; grep-verify any gap before writing. **(a) done 2026-08-11** — the migration gap was grep-verified sharper than stated: not merely absent but a *dead-ending inbound handoff* (`analytics-performance-analyst` line 112 routes migration diagnosis to `seo-technical-auditor`, which had no migration content). Shipped Critical Rule 10 + an "A Migration Moves Every Signal at Once" section + a Site Migration Runbook deliverable + a migration-signal-retention metric to both dual-located copies (baseline-before-cutover, one-to-one 301 map, one-hop rule, move-all-at-once, don't-fold-a-rewrite-into-the-move, weeks-to-reprocess / redirects-stay-a-year, move-type blast-radius table). Ideas-only; the source's uncited "30–70% loss" figure not adopted; all facts cited to three Google primary docs read 2026-08-10; lint 2/2, copies diff-identical. **(c) done 2026-08-10** — grep-verified its two named ideas as **non-gaps** (`content-refresh`/decay already owned comprehensively by `seo-content-optimizer`; pillar/hub-and-spoke architecture already `content-blog-strategist`'s own Rules 2/4/8 + its Pillar Content Architecture Roadmap deliverable), so duplicating either would collide with an existing owner. Shipped the grep-verified **adjacent** gap in the flagged never-enhanced agent instead: its flagship Rule 7 prescribes "240 cluster posts" with **no quality floor**, and Google's *scaled content abuse* policy lived in the repo only in a single AEO-playbook line the blog strategist never reads. Critical Rule 9 + a "The Plan's Volume Is a Ceiling, Not a Quota" section + a Publish/Hold/Cut gate deliverable + a value-not-volume metric to both dual-located copies; scaled-content-abuse + people-first quoted/cited to Google primary docs (Spam policies, Creating helpful content) read 2026-08-10; the retired Helpful-Content-System "site-wide drag" language flagged as no longer documented; seam drawn to `seo-content-optimizer`; lint 2/2, copies diff-identical. **(b) done 2026-08-11** — the overlap analysis ran first, as the note demanded, and split the item cleanly: the source's five-layer **method** (confirm-real → localize → page → technical → external, hypothesis-before-fix) **resolved as a non-gap** — `analytics-performance-analyst`'s "Reading a Change Before Explaining It" owns the site-level method (candidate mechanisms, contradicting evidence, smallest distinguishing check, `unresolved` as a verdict) and `seo-content-optimizer`'s decay triage owns the page-level version — so neither was rebuilt. The **real, grep-verified gap** was the SEO-native *sitewide cause taxonomy* the method runs over: `manual action` / `core update` / `algorithm update` returned **zero** hits repo-wide, `seo-content-optimizer` routes site-level causes *away* ("the page is not the story"), and the marketing generalist never names an algorithm update or a manual action. **Owner decided: `seo-technical-auditor`** — the site-level SEO diagnostician, already the migration-handoff destination, already owning two of the branches (indexing, migration); a sitewide-drop differential is the natural *router above them* that fills the two holes. Shipped **Critical Rule 11** + a **"The Whole Site Dropped: Name the Class Before You Audit"** section + a **Sitewide Traffic-Drop Diagnosis** deliverable + a **sitewide-drop class-before-fix** metric to both dual-located copies: a localization table where the segment names the owner (the branded-vs-non-branded cut), the two report-read causes (Manual Actions + Security Issues checked first, binary, remedy = reconsideration request) and the algorithm-update read held honest by timing-is-a-hypothesis + no-switch-to-flip, a deploy-is-the-prime-suspect read, a certainty-and-cost ordering that puts external causes last, and a two-classes-both-fit guardrail. The *method* is explicitly deferred to `analytics-performance-analyst`, not rebuilt, so the two never contradict on "how to diagnose." Ideas-only; frame credited to rampstackco (MIT, verified 2026-08-11); manual-action / security-issue / core-update behavior cited to Google primary docs read 2026-08-11; the class-before-fix framing, certainty-and-cost ordering, deploy-is-prime-suspect read, and two-classes guardrail are ours. Lint 2/2, copies diff-identical. **The rampstackco watch item is now fully worked (a) + (b) + (c).** — **item closed 2026-08-14** on the content/SEO scout run that was to have read it: re-checked for movement since 2026-08-10 (none in the SEO tree), and all three things it was held for are shipped or found already owned. No longer an open lead; the collection stays worth watching for *new* skills, not for this item. **Fully closed 2026-08-30:** the remaining `internationalization` skill (plus `references/locale-checklist.md`) was read on the content/SEO scout run and co-credited in the `seo-local-and-international` enhancement — its mirror/subset/local parity patterns and "update propagation is the hardest part" framing were carried as ideas; its layer framework, RTL/CSS material and TMS payback threshold were left where they belong.
- [x] Read the rest of the `ad/` corpus in [aaron-he-zhu/aaron-marketing-skills](https://github.com/aaron-he-zhu/aaron-marketing-skills) (2.5k★, Apache-2.0, pushed 2026-08-11) on a future **paid** run. Its `budget-pacing-monitor` shipped to `paid-media-budget-optimizer` on 2026-08-11; the mine is not worked out. Two things are worth a look **only if a gap grep-verifies first**: (a) its treatment of the **learning phase as a first-class state that gates action** — after 2026-08-11 we now reference learning resets in exactly two places (the new pacing section; `paid-media-ppc-strategist`'s spend-change gate) and **no agent owns the state itself** (what re-enters learning, roughly how long it runs, which reads are invalid inside it, and the fact that the fix for a pacing gap is itself the thing that resets it); (b) its `bid-strategy-planner` against `paid-media-ppc-strategist`'s bidding ground. The repo's own shape does **not** transfer — bilingual EN/ZH bodies, a `memory/` workspace, `ledger.py` connector scripts, repo-specific veto scaffolding — so ideas only, and Apache-2.0 notice requirements apply to any direct adaptation. Note this source is *also* still on `watch` from 2026-07-30 for its 16-skill email corpus (`protocol/consent-registry` resolved 08-04; the opt-down ladder shipped 07-30). **(a) done 2026-08-11** — gap grep-verified sharper than the note: the two caveats (budget-optimizer "Learning reset", creative-strategist "learning period") are *local reads*, and `learning limited` / `learning status` / `conversion cycle` / `re-enters learning` returned zero hits repo-wide, so no agent owned the state. **Owner decided: `paid-media-ppc-strategist`** (it holds the spend-change gate — the authorization for the very edits that reset learning — and its *one variable, one verification window* rule needs the reason its clock cannot start at the change). Shipped Critical Rule 9 + a "The learning phase: when the number isn't real yet" subsection + a learning-state metric to both dual-located copies. Three disciplines ours: **the recursion** (the fix is itself the reset, so you can't repair and read in the same window), **the doom loop** (reacting to an in-learning read with another edit resets again), and the **B2B conversion-volume inversion** (Meta's ~50-events/week exit is a consumer-volume criterion a demo-request ad set may never reach → set the window to the conversion cycle, or optimize on a higher-funnel event). Google facts quoted from two primary docs read 2026-08-11; Meta figures from Help Center text (pages render as JS to the fetcher, flagged in-file); ideas-only, no text adapted; lint 2/2, copies diff-identical. **(b) done 2026-08-11** — read `ad/orchestrate/bid-strategy-planner/SKILL.md` + its `references/bid-strategy-matrix.md` in full. Three gaps grep-verified distinct from (a): conversion volume was cited **only for learning-phase *exit*** (line 81), never as the pre-launch floor that decides which strategy is viable; tCPA was derived from **LTV (the affordable ceiling) with no *starting* target anchored to trailing-achieved performance** (the too-low-target-throttles discipline was absent); and `portfolio bid` existed repo-wide **only as the budget optimizer's budget-pooling *hazard*** (line 100), never as the bidding mechanic of pooling a thin campaign's signal. **Owner: `paid-media-ppc-strategist`** (its bidding ground; the new section sits under *Operating a Live Account*, upstream of the (a) learning-phase subsection — that one reads a live automated strategy, this one chooses it and sets its target before launch). Shipped **Critical Rule 10** + a **"Signal before strategy: what your conversion volume lets you bid"** subsection + an extended Bidding Strategy Framework deliverable + a signal-fit-bidding metric to both dual-located copies. **The reconciliation is ours and load-bearing:** the agent's LTV identity is kept intact by making **LTV the ceiling and trailing-achieved the starting point** (start at achievable, step toward the ceiling after each learning exit) — extends the identity, doesn't contradict it. Also ours: the **B2B inversion** (a demo-request campaign can take a quarter to clear a floor a consumer campaign clears in a day → the floor is the binding constraint) and the **pool-the-signal-not-the-budget** seam (portfolio bid strategy ≠ shared budget). Google figures quoted from primary docs read 2026-08-11 ([About Target CPA bidding](https://support.google.com/google-ads/answer/6268632) — "at least 30 conversions" recommended for evaluation, flagged as a recommendation not a hard gate; a too-low target "may cause you to forgo clicks ... resulting in fewer total conversions"; [Target ROAS requirements](https://support.google.com/google-ads/answer/6268637) — "at least 15 conversions in the past 30 days" on Search/Shopping). Ideas-only, written from scratch, Apache-2.0 notice requirements noted, no text adapted; no CPA/ROAS/volume figure of our own asserted; lint 2/2, copies diff-identical. **Both named threads (a)+(b) now worked.** The remaining `ad/` siblings (`placement-exclusion-manager`, `fatigue-frequency-manager`, `attribution-reconciler`, `conversion-value-mapper`) were **not** mined this run — re-openable by a future paid scout only if a gap grep-verifies against an existing owner first. — **done 2026-08-19.** Re-surveyed on the organic-social scout run. The audience-mismatch read from 2026-08-07 held for the bulk of the roster (creator/DTC-shaped, no paid coverage), but one slice survived it: `skills/podcast-and-audiograms`, whose retention-led clip selection and unusually disciplined `references/scope-and-connections.md` (explicitly disclaiming recording, transcription, hosting and show strategy, each routed to a named sibling) shaped **Critical Rule 6** and the nine-agent boundary in **Rule 10** of the new [`social/social-podcast-strategist.md`](../social/social-podcast-strategist.md). MIT, ideas-only, co-credited in-file.
- [ ] Licence re-check on a future **paid** run: [portermetricsample/marketing-skills](https://github.com/portermetricsample/marketing-skills) (18★, pushed 2026-08-10) ships an on-thesis `google-ads/budget/pacing/SKILL.md` and **asserts no licence**, so it was ideas-only and unread this run. It is actively maintained; if it adopts a permissive licence, its Google Ads corner is worth reading properly. Cheap check, low priority. **Re-checked 2026-08-16 (GitHub license API: still `null`) — still ideas-only and still unread.** It also ships a `meta-ads/account-safety/SKILL.md` that was directly on the 2026-08-16 ad-policy thesis and would have been a fourth sighting; it was declined on licensing, not on merit. Item stays open.
- [ ] SCOPE QUESTION — **is a "grade the marketing function itself" persona in scope for this repo?** Every one of our 76 agents is a practitioner persona that produces marketing work; none assesses whether the function is any good. [msdanyg/pmm-impact-audit-skill](https://github.com/msdanyg/pmm-impact-audit-skill) (MIT, published 2026-08-08) is the most serious argument that the gap is real: 19 criteria across five layers, scored only from evidence the user supplies, with outcome scores **capped when no attribution mechanism exists** and every gap tagged with a fix owner that is never defaulted to PMM. Those three rules are, independently, disciplines this repo already arrived at (four-state control model and evidence-coverage-graded-separately, 07-26; the incrementality precondition, 08-07), which is what makes it worth a decision rather than a dismissal. **The prior question is whether an assessment persona belongs here at all** — the same class of architectural call declined for a drafting-process persona on 2026-07-30 — and it is a maintainer call, not a scout call. Do not adopt on sight: the repo was one day old and 0★ when surveyed; re-check it has survived contact first. **Re-checked 2026-08-24: it has not** — still 0★ with no push since 2026-08-08, so the stated precondition is unmet and the item stays a maintainer call.
- [x] Evaluate **claim provenance against the product itself** as an evidence source for `product-marketing/pmm-messaging-architect`. Rule 4 grades every claim against the evidence base and the Proof Point Library holds the evidence, but the *sources* we name are customer data, benchmarks, and studies — never the product's own source of truth, which is the one form of proof a software company always possesses and the only one that catches "the site says we do X and we do not." Surfaced 2026-08-08 by [cagatayuncu/marketing-machine](https://github.com/cagatayuncu/marketing-machine) (MIT, 9★, created 2026-08-05), which scans a codebase for product truth and refuses copy that asserts an unsupported capability. **Not straightforward:** it presupposes the marketing agent has repository access, which is the same advisory-vs-executable boundary this repo has declined a dozen times — so the question is whether an *advisory* version exists (a claim-to-source-of-truth mapping the agent requires a human to fill, with unverifiable claims marked rather than a scan it runs). Three days old with no track record; re-check before acting. — **DONE 2026-08-18.** The advisory version does exist and the source itself defines it: its Principle 3 is *"a scanner sees a symbol exists; it cannot see the feature works — the draft waits for a human,"* which is the advisory boundary intact. Re-verified survival first (MIT, **12★** up from 9, pushed 2026-08-11, not archived — grown, survived contact). Shipped to both copies of `pmm-messaging-architect`: **Rule 11** + a **"Verifying the Claim: Does the Product Actually Do This?"** section + a **Capability Claim Register** deliverable + a **Capability Claim Integrity** metric. The register is a human-filled claim-to-source-of-truth mapping graded WORKS/PARTIAL/ABSENT/LIMITS with *existence never rounds to works* — the agent names the source and owner, it never scans. Value-add over the source: capability-existence named as an evidence class distinct from the outcome proof Rule 4/8 own, the entitlement/tier grade consumed from `pmm-pricing-packaging-strategist` + `pmm-agent-readiness-strategist` rather than re-derived, and ABSENT-routes-to-positioning / live-claim-is-a-compliance-finding seams. Gap grep-verified (`source of truth` = 0 in both copies); ideas-only; no figures asserted; lint 2/2, copies diff-identical.
- [x] Give `product-marketing/pmm-customer-advocacy` **Rule 7's missing method — review-solicitation integrity**. Rule 7 says "comply with FTC review guidelines and analyst platform policies" and stops: the repo's signature *control named, instrument absent* pattern. Gap grep-verified 2026-08-12: **`16 CFR`, `Part 465`, `fake review`, `review gating`, `review suppression`, `insider review`, `testimonial disclosure`, and `Endorsement Guide` return zero hits repo-wide**; `ops-legal-compliance` Rule 4 covers FTC advertising standards only as truthful-claims-plus-affiliate-disclosure, and the only adjacent content anywhere is `pmm-messaging-architect`'s note that G2 labels incentivized reviews (added 2026-07-31 for a *vocabulary-mining* purpose, not a compliance one). The 08-12 ship added the platform-policy half (G2's $100 cap, the incentivized label, eligibility never conditioned on sentiment, disclose-to-G2) inside the compensation passage — the *solicitation* half is still unowned. **The specific practice to rule on:** [the-nam-shub/e5-real-skills](https://github.com/the-nam-shub/e5-real-skills) (**unlicensed — ideas only**) teaches gating review requests behind an NPS filter (solicit only respondents scoring ≥7), which is a widely-taught B2B practice and also precisely the selective-solicitation pattern review platforms and the FTC's 2024 Rule on the Use of Consumer Reviews and Testimonials address. Our agent currently says nothing either way, so a user following the market's default advice gets no warning from us. **Decide the seam first:** advertising-law interpretation is `ops-legal-compliance`'s (it owns FTC ground and the consent record shipped 2026-08-04), while *how you ask for reviews* is the advocacy program's — so this is likely a short method on the advocacy agent with the legal call deferred, mirroring the certify-vs-verdict seam used on 2026-08-03 and 08-06. **Verify the rule text against the FTC's own primary source before writing anything** — do not assert its provisions from secondary summaries, and flag any contested reading. — **done 2026-08-12.** Gap re-verified (terms still appeared only in backlog/log/scout-ledger text, never in the agent). Seam confirmed and taken as the certify-vs-verdict pattern: the advocacy program owns *how you ask*, `ops-legal-compliance` owns the legal determination. Shipped an upgraded **Rule 7** + a **"Soliciting Reviews Without Gating the Ask"** section + a checked **G2/Capterra Review Strategy** deliverable + a **Solicitation Integrity** metric, both dual-located copies. The section names **review gating** (NPS/CSAT-screening the public-review ask) as the market default and routes it to counsel as a *named exposure*, not a settled ban — the FTC's own posture. Three disciplines: **timing vs. screening**, the **§465.4 incentive bright line**, and **§465.7 no-rating-based-suppression**. **Primary-source verified 2026-08-12** — §465.4/§465.7 regulatory text from the [official CFR PDF](https://www.govinfo.gov/content/pkg/CFR-2025-title16-vol1/pdf/CFR-2025-title16-vol1-sec465-6.pdf) (eCFR + FR + FTC HTML/PDF routes were all bot-blocked; verified via govinfo + the FTC [Q&A](https://www.ftc.gov/business-guidance/resources/consumer-reviews-testimonials-rule-questions-answers) fetched with a browser UA, verbatim *"could violate the FTC Act,"* effective **October 21, 2024**). The gating-is-unlawful reading **flagged unsettled, not asserted**; §465.4/§465.7 quoted as bright lines. Ideas-only; `the-nam-shub/e5-real-skills` **unlicensed, no content reused**; the framing and seam are ours; no penalty figure asserted. Lint 2/2, copies diff-identical, 0 broken links. The sibling item below (scoped/expiring advocacy consent record) left for a future run.
- [x] Give `product-marketing/pmm-customer-advocacy` a **scoped, expiring consent record** for advocacy assets. Deliberately left out of the 2026-08-12 ask-load ship (one change per run) and distinct from it: that section governs *how often* you ask, this governs *what they agreed to and for how long*. Gap grep-verified 2026-08-12 — `logo rights`, `quote approval`, and `approval to publish` return zero hits repo-wide, and the single adjacent line is `content-case-study-producer`'s per-asset "customer approval documentation confirming data accuracy and permission to publish," which covers one asset at one moment. The unowned discipline: what the customer agreed to (name, title, logo, quantified metrics, video, use in paid ads), on which surfaces, for how long, and what happens when the approved *metric* — a dated claim about their business — is still on the site three years later, or when a repositioning re-frames the story they approved. Also the named-vs-anonymized fallback for customers whose legal team will never approve attribution. Surfaced 2026-08-12 by the consent-flow discipline in [lpalokan/bmad-marketing-growth](https://github.com/lpalokan/bmad-marketing-growth) (MIT, "consent flow is legal, not optional", legal-reviewed template per region) and the `Legal/logo` gate in [LeadMagic/gtm-skills](https://github.com/LeadMagic/gtm-skills) (MIT) — ideas only. **Decide the seam first** between this agent (owns the relationship and the standing record), `content-case-study-producer` (owns per-asset approval, already), and `ops-legal-compliance` (owns the consent record shipped 2026-08-04, which is contact-level marketing consent — a different object from publication rights). — **done 2026-08-12** (later run). Gap re-verified (terms still 0 outside bookkeeping; `usage rights` only on `social-influencer-partnerships`, a different object). **Seam decided in-file, three records / three owners:** this agent owns the standing rights record (scope/surface/term/re-consent), `content-case-study-producer` owns per-asset publication approval (the point-in-time gate this ledger aggregates, not duplicates), `ops-legal-compliance` owns the contact-level marketing-consent record (may we *contact* — a different object from publication rights); the record lives in the CRM/advocacy platform, **not** in `templates/brand-context.md` and **not** as a new repo artifact. Shipped **Critical Rule 10** + a **"Rights Are Scoped and They Expire: The Advocacy Consent Record"** section + an **Advocacy Rights & Consent Record** deliverable + a **Rights Coverage and Freshness** metric, both dual-located copies. Consent scoped **per element × per surface**; four disciplines ours — expiring-metric review date, re-consent on a change of frame, the stored named-vs-anonymized decision, and the seam. Both sources re-verified live + MIT this run; ideas-only, no content reused; **no legal provision asserted** (every legal call defers to `ops-legal-compliance`); no figure asserted. Lint 2/2, copies diff-identical, 0 broken links.
- [x] Give `email/email-deliverability-specialist` an **engagement-signal validity discipline** — the agent made every list decision (warming cohorts, inactive identification, suppression, sunset, its `(opens + clicks) / delivered` engagement metric, its low-engagement troubleshooting branch) on an instrument that changed meaning without being renamed. Gap grep-verified 2026-08-13: `Mail Privacy Protection`, `MPP`, and `tracking pixel` returned **zero** hits repo-wide while `open rate` appeared in six decision-bearing agents. — done 2026-08-13 (Critical Rule 9 + "The Engagement Signal Is Machine-Contaminated" section + an Engagement Signal Integrity Audit deliverable + three rewritten/added success metrics, both dual-located copies. Ideas-only; surfaced by justinwilliames/orbit-for-claude and independently by Mailneo/skills, both MIT; every mechanism cited to Apple / Microsoft Learn / HubSpot / Klaviyo / RFC 8058 primary docs read 2026-08-13; no prevalence figure asserted).
- [x] Re-base the **downstream consumers** of the contaminated engagement signal, flagged by the 2026-08-13 deliverability enhancement and deliberately left out of it (one change per run). Three files still teach decisions on raw opens: `email/email-automation-engineer` branches nurture paths and scores leads on them ("after 5 emails opened in 7 days → higher engagement path"; "after 4 weeks with no opens → re-engagement"), `email/email-newsletter-growth-strategist` defines its entire subscriber tiering by open rate (50%+ / 25-50% / <25%, and "2+ editions unopened → remove from list"), and `email/email-copywriter` decides subject-line A/B tests on open rate with a "≥10% difference" significance rule. Each needs the Rule 9 evidence tiers applied to its own decision, plus the under-powering argument for the copywriter. Also check `content/content-newsletter-curator` (send-time optimization "5-15% higher open rate") and `sales/sales-outbound-strategist` (open-rate targets and "Tuesday-Thursday 9am-11am for best open rates"), both of which read timing off an instrument whose timestamps are proxy fetches. — **PARTLY WORKED 2026-08-13** (`email-automation-engineer`, the highest-consequence consumer — the one Rule 9 explicitly handed the *routing* blast radius to). Shipped a new **Critical Rule 9** + an **"An Engagement Event Is an Input; a Score Is an Action"** section + a **Scoring Signal Integrity** success metric + a caveat on the open-rate metric + five re-based decision points (point values open=0 / bare-click waits for a session, the two behavioral triggers, the nurture-branch promotion, the implicit-scoring example), to both dual-located copies. The distinct framing vs. the deliverability agent: a score/branch/threshold is an *action*, so acting on a machine event spends a sales call and corrupts the model's training data — and the re-engagement leg is broken the *other* way (proxies keep opens firing, so an opens-only sunset never triggers), so exit logic keys off silence-across-all-signals. Consuming agent: references the deliverability agent's evidence tiers rather than re-deriving them, keeps scoring math with `analytics-marketing-ops-architect`, no new external claims. Lint 2/2, copies diff-identical, 0 broken links. ****PARTLY WORKED 2026-08-13** (`email-newsletter-growth-strategist`, the second consumer — the one that both tiers the whole base on opens *and* bills a paying sponsor on the number). Shipped a new **Critical Rule 9** + a **"The Engagement Tiers Rank Subscribers on a Signal Machines Also Generate"** section + the engagement-segmentation tier block rebuilt on the four evidence tiers + three re-based sponsor-facing decision points + a corrected Rule 1 + two metric caveats, both dual-located copies. The distinct framing vs. the two prior consumers: this agent owns **content-investment misallocation** (a proxy-opened mailbox sits atop the "highly engaged" tier that premium content and monetization flow to) and a **commercial-accuracy problem no other consumer has** — the pitch, the CPM-on-readers price, and the "how many opens" report bill a paying third party against a proxy-inflated figure, re-based to quote verified reach and name the instrument. Consuming agent: references the deliverability agent's tiers rather than re-deriving them, hands the subject-line under-powering to `email-copywriter`, keeps scoring math with `analytics-marketing-ops-architect`; no new external claims or figures. Lint 2/2, copies diff-identical, 0 broken links. **PARTLY WORKED 2026-08-13** (`email-copywriter`, the third consumer and the only one that runs a *formal significance test* — so its errors stack: it decides the winner on open rate, the most contaminated signal, *and* calls a fixed "≥10% difference" significant, which is not a significance test at all). Shipped a new **Critical Rule 9** + a **"Testing Subject Lines on a Signal Machines Fake"** section + three re-based decision points (the A/B protocol, the copy-testing protocol, and the sample-size calculator) + two metric caveats, both dual-located copies. The distinct framing vs. the three prior consumers: this is a **test-validity** problem, not an action or billing one — the subject line's whole job is to move opens, so the copywriter is the *most* exposed of any consumer to the contamination, and on top of that a percentage-gap significance rule crowns noise on small sends and misses real effects on large ones. Consuming agent: references `email-deliverability-specialist` Rule 9 for the mechanism/tiers and hands the whole experiment-trust question (peeking, SRM, the ship/no-difference verdict) to `analytics-conversion-rate-optimizer`'s *Trust the Split Before the Winner*, re-deriving neither; no new external claims or figures. Lint 2/2, copies diff-identical, 0 broken links. **PARTLY WORKED 2026-08-14** (`sales-outbound-strategist`, the fourth consumer and the maximal-contamination one — cold B2B outbound into security-mature enterprises, where the anchor's "the better the logo, the dirtier" bites hardest and 100% of a send is scanner-eligible). Shipped a new **Critical Rule 9** + an **"Opens Measure the Scanner, Replies Measure the Buyer"** section + four re-based decision points (the send-time cadence line, the A/B send-time test, the winner-archiving line, the timing-patterns line — all moved from opens to replies-per-delivered) + two rebuilt Success Metrics, both dual-located copies. The distinct framing vs. the three email consumers: this is the only surface with a **native confirmed-human signal opens lack** — the *reply* — so the reframe is constructive (optimize on replies/meetings the agent already tracks, not opens), and the send-time read is uniquely broken because a scanner prefetches within seconds of delivery, so a "best open time" is mostly the SDR's own send clock echoed back. Also caught a pre-existing **internal inconsistency**: Response Rate was defined as "% of *opens* that receive a reply" with a 1-5% cold target, which is impossible — reply-over-opens (opens = 8-15% of sends) would fall below the 0.5-2% meeting rate — so the targets were always per-delivered; corrected the denominator to **delivered**, leaving every figure untouched. Consuming agent: references `email-deliverability-specialist` Rule 9 for the mechanism/tiers, keeps lead-scoring math with `analytics-marketing-ops-architect`; no new external claims or figures. Lint 2/2, copies diff-identical, 0 broken links. **DONE 2026-08-14** (`content-newsletter-curator`, the sixth and last consumer). Shipped a new **Critical Rule 9** ("The Send-Time and Engagement-Tier Loop Reads the Reader Off a Proxy, Not the Reader") + a **"Reading the Reader Off a Proxy: Send Time and Engagement Tiers"** section + five re-based decision points (Rule 3 subject-line pointer, Rule 6 send day/time, Rule 8 inactivity definition, the engagement-tier deliverable, the send-time deliverable, the Month-2 A/B calendar line) + two corrected Success Metrics (the flagged "5-15% higher open rate" send-time metric re-based off opens entirely; the Open-rate metric given the name-the-instrument / directional-only caveat), both dual-located copies. The distinct framing: this agent is the literal instance the anchor warns about — `email-deliverability-specialist` Rule 9 names *send-time optimization* as the canonical proxy-derived decision (MPP prefetches the pixel on its own schedule, so an open timestamp is a relay's clock), and the newsletter's whole loop was built on it; the constructive move is that the newsletter owns confirmed/probable-human signal downstream (CTA-click-to-session, MQL conversion, both already tracked). The tier/sunset error is the damaging one — a proxy opens a dead address forever, so an opens-based sunset never fires. Consuming agent: references the deliverability tiers rather than re-deriving, points subject-line A/B validity to `email-copywriter` and experiment-trust to `analytics-conversion-rate-optimizer`, keeps scoring math with `analytics-marketing-ops-architect`; no new external claims, one contaminated benchmark figure removed. Lint 2/2, copies diff-identical, 0 broken links. **This item is now fully complete — all six downstream consumers re-based.**
- [ ] Read the `revops/` and `leadership/` trees of [modest-curator478/claude-skills](https://github.com/modest-curator478/claude-skills) (3★, MIT, pushed 2026-08-12) on a future **ops** run. Surfaced 2026-08-13 and left on `watch`: `martech-contract-auditor` and `free-trial-designer` grep as unowned here, and its `leadership/` tree (`marketing-budget-defense`, `marketing-qbr`, `sales-marketing-sla`) is a **second independent arrival** at the open "grade the marketing function" SCOPE QUESTION filed 2026-08-08 — none of our 72 personas defends a budget or runs the QBR. Grep-verify any gap first. **PARTLY DONE 2026-08-17:** the `revops/abm-campaign-builder` half was read on the PMM/GTM scout run and its account-selection, capacity-filter and no-MQLs measurement framing are credited in the new `abm-account-based-strategist`. The `leadership/` tree is still unread and still feeds the open grade-the-function SCOPE QUESTION; `martech-contract-auditor` and `free-trial-designer` remain ungrepped. **Re-checked 2026-08-24** (PMM/GTM scout run): the repo is alive — pushed 2026-08-24, now 4★ — and the `leadership/` tree is still unread. Not opened, because the question upstream of it is line 85's maintainer call. **`martech-contract-auditor` CLOSED 2026-08-26** (email/analytics/ops scout run): read in full and judged **not** the gap — it is a renewal-negotiation leverage skill (usage-vs-capacity, vendor fiscal-quarter timing, credible alternatives, multi-year commitment, bundle gaps, tenure → a one-page negotiation brief with ranked asks and a walk-away), which is `ops-financial-tracker` ground, and its thresholds (70% capacity, 15-25% multi-year discount, 10-20% quarter-end bonus) are unsourced round numbers. Re-filed as an `ops-financial-tracker` enhancement lead below. The ADD that run went to the *portfolio* half of the same territory, which nobody owned: `analytics-martech-stack-strategist`. **Still open:** `free-trial-designer` ungrepped, and the `leadership/` tree still feeds line 85's SCOPE QUESTION.
- [x] Give `seo/seo-content-optimizer` a **page-credibility assessment method (E-E-A-T)**. `E-E-A-T` appeared in exactly four places repo-wide and every one was a passing mention (`EEAT`, `information gain`, `originality`, `first-hand`, `author bio`, `semantic gap` = zero), which made the sitewide-drop section shipped 2026-08-11 a **dead-ending handoff**: it routes an algorithm-update class to "the slow content and E-E-A-T self-assessment that `seo-content-optimizer` and `content-blog-strategist` own," and neither file used the vocabulary. Surfaced 2026-08-14 by `eeat-audit` in [inhouseseo/superseo-skills](https://github.com/inhouseseo/superseo-skills) (Apache-2.0) with an independent equivalent in [AgricIDaniel/claude-seo](https://github.com/AgricIDaniel/claude-seo)'s `eeat-framework` (MIT) — ideas only. — done 2026-08-14 (Critical Rule 10 + "Credibility Has Four Parts, and Only Two of Them Are the Page's" + a Page Credibility Assessment deliverable + a Credibility routing metric, both dual-located copies). **Owner decided by the repo's existing seam** — the Blog Strategist owns prospective prevention (its 2026-08-10 quality-floor section), this agent owns pages that already exist. **Both sources' composite scores deliberately not adopted** (a 40-point sum; a weighted 0–100): Google documents that E-E-A-T "isn't a specific ranking factor" and raters "have no control over how pages rank," so the output is a routed work list, and **trust is implemented as a gate rather than a heaviest weight** because any total lets Expertise outvote a failed Trust check — the same defect fixed in lead scoring on 2026-07-30. The four-way owner routing (authoritativeness is not fixable on the page), the ghostwriting seam (*the voice may be ghostwritten; the experience must be the byline's own*, pointing at `content-thought-leadership-ghostwriter` Rule 2's interview as the harvesting instrument and extending it past executives), the B2B YMYL classes, unknown-never-passes, and the no-recovery-date guardrail are ours. Facts cited to Google primary docs read 2026-08-14; lint 2/2, copies diff-identical.
- [x] Read [Bhanunamikaze/Agentic-SEO-Skill](https://github.com/Bhanunamikaze/Agentic-SEO-Skill) (836★, MIT, pushed 2026-07-23) on a future **content/SEO** run. Surfaced 2026-08-14 and left on `watch`: 16 sub-skills and 10 specialist agents over **88 optional utility scripts framed explicitly as "evidence collectors"** — the scripts produce the evidence a judgement rests on rather than automating the judgement, which is our evidence-coverage-graded-separately-from-health rule (2026-07-26) expressed as tooling, and is adjacent to the open P2 "evaluation harness" item. Two things to read deliberately: whether that separation suggests anything portable into an advisory persona, and its **SaaS vertical strategy template** against our own B2B-SaaS framing. Nothing textually adoptable on first pass; grep-verify any gap first. — **PARTLY WORKED 2026-08-18** (the SaaS-template + `seo-competitor-pages` slice). Read `resources/templates/saas.md` and `resources/skills/seo-competitor-pages.md` in full via the GitHub API. The real gap was the **single hand-built comparison/alternatives page** as an SEO page type: `seo-programmatic-strategist` Rule 10 routes it to `seo-content-optimizer` ("who owns hand-written pages") but that file never named it, and whole-repo the page's *SEO strategy* was owned by nobody (grep-verified: `comparison page`/`alternatives page`/`nominative`/`SERP-intent gate`/`switched from` = 0 on the optimizer). Shipped a **"The Comparison Page Is Where the Trust Gate Is Hardest to Hold"** section + **Comparison & Alternatives Page Spec** deliverable + a trust-discipline metric to both dual copies of `seo/seo-content-optimizer.md`. Ideas-only, MIT-credited; the source's single-vendor-survey conversion figures not adopted; the SERP-intent gate, the fairness-as-Trust-gate reframing, and the seams are ours; lint 2/2, copies diff-identical. **Still unread:** the other 15 sub-skills / 10 agents / 88 evidence-collector scripts (architecture already assessed as covered by our evidence-coverage rule; `seo-github` is out of B2B-marketing scope) — the evidence-collector separation vs. the open P2 "evaluation harness" item stays on `watch`. — **done 2026-08-25 (content/SEO scout run). Nothing adopted.** Its 10 agents, 16 skills and 10 reference files were enumerated and read at index level: every on-thesis piece (`seo-aeo`, `seo-geo`, `seo-competitor-pages`, `seo-hreflang`, `seo-programmatic`, `eeat-framework`, `link-building`, `cwv-thresholds`, `schema-types`) is a discipline this repo already ships, several of them enhanced *after* its last push (2026-07-23) — the comparison/alternatives page on 2026-08-18, the E-E-A-T decomposition already in `seo-content-optimizer` Rule 10, the AEO effect-size bound on 2026-08-23. Its one genuinely distinctive corner is **GitHub as a ranked discovery surface** (`seo-github`, `github-ranking-factors`, `readme-audit-rubric`), which is `devmkt-developer-audience-strategist`'s repo-positioning ground and too narrow to justify a change. The "88 evidence-collector scripts" note that put it on watch is the executable-tooling direction this repo has declined repeatedly; it stays relevant only to the open P2 evaluation-harness item, which is a maintainer-scoped structural change, not a scout ship.
- [x] Evaluate `semantic-gap-analysis` and `expert-interview` in [inhouseseo/superseo-skills](https://github.com/inhouseseo/superseo-skills) (Apache-2.0) on a future **content/SEO** run — the two un-mined skills from the 2026-08-14 source. `semantic gap` and `information gain` both grep to **zero** repo-wide, and `seo-content-optimizer` Rule 3 mandates "NLP analysis of top-ranking competitors… entity mentions, and semantic patterns" without a method behind it, which is the repo's *control named, instrument absent* pattern again. `expert-interview` is the natural companion to the Experience-harvesting practitioner interview shipped 2026-08-14 and may already be covered by `content-thought-leadership-ghostwriter` Rule 2 — **check that overlap first**, and do not duplicate it. Apache-2.0 notice requirements apply to any direct adaptation; ideas-only otherwise. — **`semantic-gap-analysis` slice done 2026-08-14** (both dual-located copies of `seo-content-optimizer`: a "Coverage Parity Is the Floor, Not the Finish" section operationalizing the existing Rule 3 — no new Critical Rule — plus a Topical Coverage & Information Gain Map deliverable, a Coverage-and-gain balance metric, and a de-stuffing tweak to On-Page Optimization Specifications). Gap re-verified before writing (`semantic gap`/`information gain` = 0 real method hits). The two-opposite-failures framing (coverage gap vs. sameness trap), the consensus-vs-outlier split, the parity-flips-the-question construction, and the seam tying information gain to the Experience letter shipped the same day are ours; information-gain definition quoted from the primary patent [US11354342B2](https://patents.google.com/patent/US11354342B2) (Google LLC, granted 2022-06-07); the industry "dominant 2026 signal" claim + visibility figures flagged unverified and not asserted; Apache-2.0 source ideas-only, no adaptation. Lint 2/2, copies diff-identical. **`expert-interview` slice done 2026-08-14** (both dual-located copies of `content-thought-leadership-ghostwriter`: new **Critical Rule 9** + a **"The Expert Interview Method"** section + an **Expert Knowledge Document** deliverable + a **First-hand density** success metric). Overlap checked first as instructed: the interview *targets* existed (Rule 2 names philosophy/contrarian/experience) but the *method* did not — `question bank`, `depth over breadth`, `knowledge document`, `scope limit`, `surprise`, and `who should not` grepped to **zero** on the file, and the only repo "question bank" is the sales-discovery one in `sales-enablement` (a different object — buyer discovery, not expertise extraction). This is the *control named, instrument absent* pattern: both Rule 2 and today's `seo-content-optimizer` credibility/coverage work reference an interview as their Experience/information-gain harvesting instrument — the latter explicitly routes its practitioner interview *here as "the model," to be extended past executives* — yet neither supplied the how. Shipped the session discipline (2–4 questions at a time, depth over breadth, calibrate to interviewee) and the **four-pull taxonomy** (contrarian, specificity, *surprise*, *scope-limit/"who should NOT"* — the last two net-new repo-wide), extended the instrument past executives to the **practitioner/SME** (perspective vs. performance), drew the seam to `content-case-study-producer` (the *customer's* story is a different interview and owner — no conflation), kept the one-extraction rule, and made the output a reusable **knowledge document, not a transcript**, so `seo-content-optimizer`'s pointer now resolves to a real method. `seo-content-optimizer` deliberately **not** re-touched (already edited twice today; it references, doesn't own, the instrument). The executive-vs-practitioner split, the seam, the one-extraction rule, the no-manufacture boundary, and the B2B tilts are ours; the four archetypes + reusable-doc output surfaced by `expert-interview` (Apache-2.0, license re-verified 2026-08-14, ideas-only, no direct adaptation). No figure asserted; lint 2/2, copies diff-identical, 0 broken links. **This item is now fully complete — both slices shipped.**
- [x] Extend the 2026-08-16 ad-policy discipline to the **Google/search surface** on a future **paid** run. `paid-media-social-ads-specialist` now owns pre-flight policy review and enforcement response for Meta and LinkedIn (Critical Rule 10 + "Approval Is Not Permission"), and it deliberately draws a seam rather than reaching across: **Google's rulebook stays with `paid-media-ppc-strategist`**, which today has an *operational* spend-change gate and a learning-phase section but **no policy dimension at all**. Two specifics were surfaced and not worked (one change per run): the **trademark remedy is scoped to the second-level domain** in the final URL, not to the offending ad, which makes competitor-comparison and "alternatives" campaigns — a staple of B2B SaaS search — an account-level decision; and Google's **restricted-category** and misrepresentation rules have no owner here. Grep-verify against the shipped Meta/LinkedIn section first so the two don't duplicate; the ordering rule, the four dispositions, and *approved never rounds to compliant* are already written and should be referenced, not restated. — done 2026-08-16 (same day as the ship it follows, but on a different agent and a distinct surface). Gap grep-verified — the five policy terms returned zero on the PPC strategist. Shipped **Critical Rule 11** + a **"The policy gate: running inside Google's rulebook"** section + a **Search Ad Policy Pre-Flight** deliverable + a success metric in both dual-located copies. References the shipped section's shared machinery (account-scale-first ordering, four dispositions, *approved never rounds to compliant*, quote-the-clause) rather than restating; adds what Google does differently — **two enforcement speeds** (7-day warning + three-strike ladder vs. egregious/misrepresentation suspension on detection with no warning, reaching related accounts) so the no-reverse class is screened first; **misrepresentation reframed as the policy face of the ad-to-landing-page relevance Quality Score already rewards**; and the **competitor-name asymmetry** (allowed as a keyword, an account-level bet in ad text because the restriction is domain-scoped; the reseller/informational exception turns on the landing page). Ours: the two-speeds ordering read, the misrepresentation reframe, the keyword-vs-ad-text reconciliation, and the exception-turns-on-the-destination seam. Ideas from the same MIT sources the shipped section credits; every Google position cited to primary docs read 2026-08-16 (domain-scope sentence quoted verbatim, others paraphrased-and-cited since read through a summariser); no approval/rejection/appeal/suspension-frequency figure asserted; lint 2/2, copies diff-identical, all five external URLs HTTP 200. **Line 92 (enforcement-register ownership) remains open — it is a maintainer decision, not affected by this ship.**
- [ ] Decide whether the **enforcement register** belongs to paid media alone or is a client-ops artifact. The 2026-08-16 ship gave `paid-media-social-ads-specialist` an Ad Policy Pre-Flight & Enforcement Register, and the review it performs is explicitly *not* the legal review that `client-ops/ops-legal-compliance` and `ops-quality-assurance` already run — two rulebooks, two consequence classes. That seam is stated in-file and is correct, but it leaves an open question a maintainer should answer rather than a scout: when a single ad has to clear both, **who runs the combined pre-launch gate**, and does `ops-quality-assurance`'s existing pre-launch compliance checklist need a platform-policy row that routes to paid media (rather than silently implying it covers the platforms)? Low risk either way; the cost of leaving it is a launch checklist that reads as complete and isn't. **PARTLY DONE 2026-08-22:** the concrete sub-question — does `ops-quality-assurance`'s pre-launch checklist need a platform-policy row rather than silently implying coverage — is resolved. Both dual-located copies of `client-ops/ops-quality-assurance.md` gained a **"Platform Clearance Is Not Legal Clearance"** section and a distinct ad-platform-policy-clearance row on the Legal/Compliance and Launch Readiness checklists, routing the gate to `paid-media-social-ads-specialist`'s Enforcement Register and closing the asymmetry (the seam was stated only from the paid-media side). **Still open (maintainer call):** whether the enforcement register is a paid-media-only artifact or a client-ops one, and who formally owns the *combined* pre-launch gate when one ad must clear both rulebooks.

- [x] Enhance `paid-media/paid-media-social-ads-specialist` with the paid-ABM execution layer surfaced 2026-08-17 in [coreyhaines31/marketingskills](https://github.com/coreyhaines31/marketingskills)' `skills/ads/references/abm-playbook.md` (MIT) — read in full and **deliberately not adopted into the new ABM program agent**, because it is channel-execution ground the boundary rule assigns here: LinkedIn list mechanics and the segment-mixed-lists failure (one blended list lets delivery park on the largest enterprises), Meta's identity-graph match-rate problem for work emails, acceleration campaigns against open pipeline on a reach objective rather than conversions, and frequency capping via a self-rotating engagement-exclusion audience. Verify each platform claim against primary docs before adopting; the source's ABM go/no-go thresholds (list size, deal size, budget floor) are unsourced round numbers and should **not** be carried. The one fact already crossed the seam (the ≥300-row / ≥300-match LinkedIn floor, cited in the ABM agent's Rule 7 from LinkedIn's own help page). — **done 2026-08-17** (Critical Rule 11 + "The Account List Is a Media Object" section + Paid ABM Audience Construction & Delivery Plan deliverable + tier-segmented-delivery metric; all four mechanics shipped, each platform claim verified against primary Meta/LinkedIn docs and cited with read-date, the source's go/no-go thresholds deliberately not carried, work-email penalty flagged directional; both dual copies diff-identical, lint 2/2).
- [ ] Read the `plugins/abm-orchestration/commands` layer of [gtmagents/gtm-agents](https://github.com/gtmagents/gtm-agents) (Apache-2.0) on a future **architecture**-flavoured run — `target-accounts` / `plan-plays` / `monitor-abm` expose an ABM program as three slash-commands over one persistent account set, which is the same shape as `loops/monthly-abm-loop.md`'s account ledger but surfaced as commands rather than a document a human re-runs. Relevant to the open P2 "native subagents" ([#1](https://github.com/shalintripathi/saas-marketing-agents/issues/1)) and evaluation-harness items, and to the loop library generally. Filed 2026-08-17.

- [ ] Decide whether **synthetic / AI-generated voice in marketing audio** needs a disclosure discipline, and who owns it. Surfaced 2026-08-19 while surveying [minhnv0807/ai-business-skills](https://github.com/minhnv0807/ai-business-skills), whose voice-clone and AI-avatar skills were dismissed as creator-shaped — but the underlying question is ours: `social-youtube-producer` Rule 4 already permits "AI-generated voices acceptable for tutorials" with no disclosure attached, and the new `social-podcast-strategist` disclosure rule covers *material connections* (16 CFR § 255.5), not *synthetic media*. `ops-legal-compliance` already carries an open EU AI Act Art. 50 / C2PA proposal, which is where this most plausibly belongs rather than on a channel agent. Scope before writing; do not add an agent for it.
- [x] Evaluate whether the **webinar** engine and the new podcast agent share a seam worth naming. `events-field-marketing-strategist` owns "the webinar engine" and `social-podcast-strategist` (shipped 2026-08-19) owns audio; a recorded webinar republished as an episode, and a live-recorded episode at a conference, both cross that line. Rule 10 already assigns the *room* to events and the *production* to podcast, which may be sufficient — verify with a grep before changing anything, and prefer no change to a redundant one. — **done 2026-08-22.** Grep-verified the seam was *asymmetric*, not redundant: the podcast agent's Rule 10 named only the **conference-room** direction ("a live episode recorded at a conference is their buy and your production"), while the **webinar-recording-republished-as-an-episode** direction was named by neither side — the events agent's Rule 10 seam list (6 downstream owners) omitted the podcast strategist entirely, and its "on-demand repurposing plan" / "on-demand afterlife" never routed an audio episode to the podcast feed, silently implying events owns the audio afterlife. Closed it symmetrically: the podcast Rule 10 now adds the repurposed webinar alongside the conference case (events' buy, podcast's audio edit + feed), and the events Rule 10 gains the Podcast Strategist as a named seam (the on-demand afterlife is events' to plan, the podcast feed is not events' to run). Both agents dual-located; all 4 copies edited, lint 4/4, copies diff-identical. No count/name/capability change → no discoverability sweep owed.
- [ ] On a future **social** run, check whether `social-podcast-strategist`'s Rule 9 (qualify a show on observable audience evidence, not self-reported downloads) should acquire a named method the way the repo has done for other *control named, instrument absent* rules. It currently lists the evidence classes — guest history, topic mix, comment substance — without a grading scheme. Only act if a source offers something better than an invented rubric; a fabricated scoring model would be worse than the current honest list.
- [x] **Name the reciprocal seam for the new `paid-media-sponsorship-syndication-buyer` (shipped 2026-08-23).** Its Rule 10 cedes conference sponsorship to `events-field-marketing-strategist` and any in-voice host endorsement to `social-influencer-partnerships`, but neither of those agents names it back — the same one-sided-seam pattern the 2026-08-16 enforcement-register item raised and the 08-22 QA run closed. Low risk; the cost of leaving it is that a marketer reading the events or influencer agent alone will not learn that a newsletter/podcast/community *placement* has a different owner. One line each, on a future **events** or **social** run. — **done 2026-08-23**: events `Rule 10` seam list now names the buyer as the owner of a media slot (the mirror image of a conference room), routing a "sponsor this newsletter/podcast" ask to the buyer instead of an event tier; influencer `Rule 3` now draws the reciprocal boundary the buyer's Rule 4 states — a bought slot (newsletter ad, scripted host-read, syndication unit) is media inventory owned by the buyer, not a creator partnership. Both dual pairs diff-identical, lint 4/4, 0 broken links.
- [x] **Does the repo need an owner for affiliate and customer-referral programs?** Surfaced 2026-08-23. `affiliate` grazes five agents (`email-newsletter-growth-strategist` monetization, `social-influencer-partnerships` commission links, `ops-legal-compliance` disclosure, `seo-content-optimizer`, `partner-ecosystem-marketer`) and is owned by none; `referral program` resolves only to newsletter growth. Two independent sources ship it as its own discipline — [coreyhaines31/marketingskills](https://github.com/coreyhaines31/marketingskills)' `referrals/affiliate-programs` (MIT) and [teachskillofskills-ai/DigitalMarketingPro-techshu](https://github.com/teachskillofskills-ai/DigitalMarketingPro-techshu)' `growth-engineering/affiliate-marketing` (MIT). Grep-verify the gap and decide **owner vs. new agent** before writing: a B2B SaaS affiliate program may be a section on `partner-ecosystem-marketer` rather than a persona of its own. — **done 2026-08-23. Decision: section, not a new persona.** Grep confirmed the gap exactly as filed (the five `affiliate` grazes are all sub-topics — newsletter monetization, creator comp model, disclosure, SERP competition, UTM medium — and `referral program` resolved only to *subscriber* and *community* referral). The **affiliate / partner-referral program** (third parties paid to refer customers) is unambiguously `partner-ecosystem-marketer`'s — marketing *with and through* other companies — so it landed there as a Core Mission bullet + **Critical Rule 11** + an *Affiliate & Partner-Referral Program Design* deliverable + a channel-quality metric (both dual copies; CATALYST Partnerships row updated). The counterparty test (Rule 1) still routes an individual creator-as-affiliate to Influencer Partnerships. The **customer** referral program (existing customer, one-time reward, ask spends the advocacy ledger) was deliberately **not** absorbed — it is a different motion and its owner (`growth-customer-marketing-lead` vs. `pmm-customer-advocacy`) is a maintainer call, named in-file and left open below. Ideas-only, both sources MIT-credited; no figures imported. Lint 2/2, copies diff-identical.
- [ ] **Maintainer call left open by the 2026-08-23 affiliate ship:** who owns the **customer referral program** (existing customers referring peers for a one-time reward)? It is not a partner channel — the referrer is a customer, trust is high, volume low, and the referral *ask* spends the same finite goodwill `pmm-customer-advocacy`'s ledger governs. `partner-ecosystem-marketer` Rule 11 now explicitly disclaims it and points here. Candidates: `growth-customer-marketing-lead` (installed-base motion) or `pmm-customer-advocacy` (owns the ask ledger). Decide owner-vs-section before any code; likely a section on the chosen owner, not a new persona.
- [x] **Name the reciprocal affiliate seam on the influencer agent (follow-on to the 2026-08-23 affiliate ship, same pattern as line 104).** `partner-ecosystem-marketer` gained the affiliate/partner-referral *program* on 2026-08-23 and its Rule 1/Rule 11 hand an individual creator-as-affiliate to `social-influencer-partnerships` — but the influencer agent named it *back* nowhere (grep: `partner-ecosystem` = 0 in both copies), while listing "affiliate programs" in its core mission and running a full Affiliate Partnerships playbook, so a marketer reading it alone would think it owns the whole channel. The one-sided-seam defect line 104 fixed for sponsorship, left half-done for affiliates. — **done 2026-08-24**: added a reciprocal-boundary sentence to the influencer agent's **Rule 4** (both dual copies) mirroring `partner-ecosystem-marketer` Rule 11 — signing an individual creator *as* an affiliate is the influencer agent's (the counterparty is a person), but the affiliate/partner-referral *program* as an acquisition channel (commission model, attribution window, anti-fraud controls, PRM tooling, recruitment of complementary companies) is `partner-ecosystem-marketer`'s. Self-directed seam clarification, no external source; no count/name/capability change; lint 2/2, copies diff-identical, 0 broken links.
- [ ] Read the remaining 23 event skills in [guerrilla2799/event-and-scale-os](https://github.com/guerrilla2799/event-and-scale-os) (0★, MIT, pushed 2026-08-22) on a future **events** run. Surfaced 2026-08-23; its `sponsorship-negotiation` skill was read and credited in the new sponsorship agent, and the rest (`executive-dinner`, `meeting-engine`, `invite-motion`, `roundtable-and-salon`, `run-of-show`, `event-measurement`, `artifact-eval`) is directly on-thesis for `events-field-marketing-strategist`, the repo's only events agent. Zero stars and days old — re-check it has survived contact before adopting anything.
- [ ] Re-check [teachskillofskills-ai/DigitalMarketingPro-techshu](https://github.com/teachskillofskills-ai/DigitalMarketingPro-techshu) (0★, MIT, pushed 2026-08-20) on a future run. Left on `watch` 2026-08-23: 163 skills / 24 agents, and the only whole **reputation-management** discipline in that survey (`review-strategy`, `review-management-platforms`, `review-response`). The B2B analog is already largely owned — `pmm-customer-advocacy` holds review solicitation under 16 CFR Part 465 (08-12) and the paid layer went to the new sponsorship agent (08-23) — so the residual to check is **review *response*** (who answers a public negative review on G2, and is that marketing's job or support's?), which greps as unowned. Local-business shaped and 0★; grep-verify before acting. — **partly closed 2026-08-25.** Re-checked and read this run as one of four independent arrivals at the review-platform surface; its **listing-accuracy** half shipped into `seo-link-building-strategist` (Rule 10 + the "Ranking Inside Pages You Don't Own" section), which now owns the *entry* on a third-party page and explicitly does **not** own the reviews inside it. The residual is therefore narrower than filed and still open: **who answers a public negative review — marketing or support — and does the answer belong to `pmm-customer-advocacy` (who owns solicitation, the consent record and 16 CFR Part 465) or to a support function this repo does not staff?** That is an owner question for a maintainer, not a scout ship.
- [x] **Clean up four invented benchmarks in `pmm-competitive-intelligence`'s success metrics**, left untouched by the 2026-08-24 scout ENHANCE under one-change-per-run. They pre-date the repo's anti-fabrication posture and are unsourced round numbers: *"40%+ of competitive wins credit battle cards"*, *"Target 60%+ win rate against primary competitor, 70%+ against secondary"*, *"30%+ improvement in customer perception"*, and *"10-15% overall win rate improvement in first year of competitive intelligence program"*. Also fix the internal inconsistency between Rule 5 (*15-25* win/loss interviews per quarter) and the Competitive Research Depth metric (*15-20*). Convert each to a definition, a coverage measure, or a baseline-from-your-own-first-cycles instruction — the pattern the three metrics added on 2026-08-24 follow — or cite a real source. Both dual-located copies. — **done 2026-08-24.** All four rewritten to the 2026-08-24 pattern (definition / coverage / baseline-from-your-own-cycles, no invented targets): *Win/Loss Insights Adoption* → the share of interviewed wins that credit intel, tracked as a trend not a target; *Win Rate by Competitor* → computed from the closed-deal census (never the interview sample, keeping it consistent with the Evidence Provenance metric), read as a trend with the deal count printed; *Customer Perception Shift* → direction/durability of a repeatable study's pre-post move against your own prior wave, reported with sample size and "not readable" where the sample can't carry it; *Competitive Win Rate Improvement* → trend against a baseline captured *before* the program, attributed to specific work rather than a promised first-year figure. Inconsistency fixed: Competitive Research Depth now reads *15-25* per quarter, aligned to Rule 5. Both copies diff-identical, lint 2/2, no count change (72/19).
- [x] **Same invented-benchmark cleanup for `pmm-positioning-strategist`'s Competitive Win Rate metric** — found in passing during the 2026-08-24 line-110 fix. `product-marketing/pmm-positioning-strategist.md` line 78 still reads *"Target 60%+ win rate in primary competitive scenarios"*, the same pre-anti-fabrication round number just removed from `pmm-competitive-intelligence`. Not touched this run under one-change-per-run. Convert to the same pattern (census-based win-rate *trend* with the deal count printed, no fixed target), keeping it consistent with the now-corrected Win Rate by Competitor metric on the Competitive Intelligence agent. Both dual-located copies. — **done 2026-08-25.** Rewritten to the census/trend pattern: win rate against each *named* competitor, from the closed-deal census (never a survey sample), read as a trend against your own prior cycles after the positioning shipped (no fixed threshold), each rate reported with its deal count and small-N flagged directional; carries an explicit "consistent with the census-based Win Rate by Competitor metric on `pmm-competitive-intelligence`" cross-reference. Both dual-located copies diff-identical, lint 2/2, no count change (72/19). The `sales-discovery-coach` *"Target 60%+ of qualified calls with quantified impact"* is a different agent's operational coverage target, not a market-outcome benchmark — left out of scope.
- [x] **Re-verify the ICC/ESOMAR article references against the fifth edition** (revised 2025-09-29) cited in `pmm-competitive-intelligence`'s *Three Records, Three Questions* section. The 2026-08-24 run read the **2016 edition** in full and quoted Art. 1(c)-(d), 4(a) and 6(a) verbatim from it; the 2025 edition's download is gated behind a language-selection form on [ICC's page](https://iccwbo.org/news-publications/business-solutions/iccesomar-international-code-market-opinion-social-research-data-analytics/) and was not read. The file carries an in-line note saying so. If the articles renumbered or the wording moved, update both copies and drop the note. Low risk, but the citation should not silently point at a superseded edition. — **done 2026-08-25.** The fifth edition was located as a clean, readable primary PDF on ESOMAR's own standards host ([standards.esomar.org/assets/documents/icc-esomar-code-2025.pdf](https://standards.esomar.org/assets/documents/icc-esomar-code-2025.pdf), © ICC/Esomar 2025, "Updated as of July 2025," HTTP 200) and read in full — the language-selection gate was on ICC's page, not on this host. **Every cited article had renumbered or moved**, so the citation was silently pointing at a superseded edition: 2016 Art. 1(c) → **Art. 1(d)** (now grounded explicitly in public confidence, "researchers must diligently maintain a clear distinction between research and non-research activities"); 2016 Art. 1(d) "clearly distinguish and separate" → **Art. 1(e)** ("informed … before data collection begins," "[s]eparate consent … for non-research purposes," "clearly distinguished from the research activities"); 2016 Art. 4(a) → **Art. 4(a)** reworded (identify promptly; participation "voluntary and based on clear and accurate information about the general purpose and nature of the research") with the refusal right moved to **Art. 1(a)** ("right to decline participation"); 2016 Art. 6(a)'s non-research-purpose-disclosure duty **folded into Art. 1(e)** (2025 Art. 6(a) is now a different provision, on client data transfer); and the footnote's "Article 9 of the consolidated ICC marketing code" → **"Article 7 of the ICC Advertising and Marketing Communications Code,"** its wording expanded (now naming consumer reviews and social-media postings). Both dual copies rewritten with all quotes verbatim from the 2025 edition, the URL and edition descriptor updated, and the in-line "not read" caveat dropped. Lint 2/2, copies diff-identical, 0 broken links, no count change (72/19).
- [x] **Retire the invented benchmarks in `content-copywriter`'s success metrics** — the same anti-fabrication defect cleaned from the two PMM agents on 2026-08-24/25, found dense in the content discipline. The entire Success Metrics section asserted unsourced round numbers as fact (*"8-15% conversion target"*, *"3-5% email CTR… 20-30% lift"*, *"60%+ of copy tests generate a significant winner"*, *"30-50% of all page actions"*, *"15-30%"*, *"20-35%"*, *"50%+ higher"*, *"10-15%"*), plus two more in the Identity (*"lift conversion by 3-5%"*) and A/B roadmap (*"10-20% lift potential…"*). — **done 2026-08-25.** All eight scorecard bullets + the two supporting figures rewritten to the definition / coverage / baseline-from-your-own-page pattern; no figure asserted; the *"60%+ of copy tests win"* metric (doubly wrong — fabricated *and* the peeking-inviting quota retired from `analytics-conversion-rate-optimizer` on 2026-08-10) became an A/B test *verdict* rate; experiment-trust cross-referenced to `analytics-conversion-rate-optimizer`, subject-line validity to `email-copywriter`, cross-surface coherence to `pmm-messaging-architect` (all three verified to exist). Illustrative example copy (PAS demo, sample headlines, worked hypotheticals) deliberately left intact. Both dual-located copies diff-identical, lint 2/2, old strings grep to zero, no count change (72/19).
- [x] **Content-agent anti-fabrication sweep — DONE 2026-08-27 (all 7 content personas cleaned).** `content-copywriter` was cleaned 2026-08-25; `content-video-script-writer` was cleaned 2026-08-26; **`content-case-study-producer`** was cleaned **2026-08-26** (Success Metrics section reworked to a measure-against-your-own-funnel callout + 9 bullets — sales-cycle 5-10 days / 15-20% faster, gated 40-60 MQLs / $200-400 CPL, deal-stage 25-30%, persona 30+ leads/90d, video 15-25%, recycling 8-12 pieces, portfolio 80%+, participation 70%+ — plus Rule 7's "8-12 derivative pieces" yield; accelerator/win-rate/testimonial claims now demand a control, coverage metrics set to the sales motion's own need; both dual copies synced, lint 2/2, old strings grepped to zero). **`content-newsletter-curator`** was cleaned **2026-08-27** (Success Metrics section reworked to a measure-against-your-own-history callout + 12 bullets — list growth 5-10%, open 25-35%, CTR 4-8%, unsubscribe 0.1-0.3%/0.5%, conversion 1-3%, lead-share 15-25%, lifetime/expansion 5-10%/10-15%, re-engagement 15-25%, revenue 8-12%, segmentation 20-30%/25-35%, content-mix 3-5x/2-3x all de-numbered; the 2026-08-14 open-rate contamination discipline preserved but its "25-35%" assertion dropped; the duplicated 0.1-0.3%/0.5% benchmark in Rule 8 re-based in the same pass; both dual copies synced, lint 2/2, old strings grepped to zero). **`content-whitepaper-architect`** was cleaned **2026-08-27** (all 9 Success Metrics bullets reworked to a measure-against-your-own-funnel callout + de-numbered metrics — lead volume 80-150 MQLs / enterprise 50%+ / mid-market 30%+, lead quality 25-35% SQL, earned media 5-10 mentions / 2-3 citations / 50+ shares, sales velocity 10-15 days, executive reach 40%+, credibility 70%+, speaking 2-4/yr, longevity 20-30 downloads/mo / 18-24 mo, analyst 1-2 reports — all removed; sales-velocity claim now demands a within-pipeline control, ownership seams drawn to `analytics-performance-analyst`/`comms-pr-strategist`/`comms-analyst-relations-manager`/`sales-pipeline-analyst`/`sales-enablement-content-creator`; the illustrative "41%/$2.3M" finding-headline demo left intact; both dual copies synced, lint 2/2, old strings grepped to zero). **`content-blog-strategist`** was cleaned **2026-08-27** — the seventh and last content persona, **closing this sweep** (all 8 Success Metrics bullets reworked to a measure-against-your-own-history callout + de-numbered metrics — organic traffic +30% YoY, MQL share 20%+/15-25%, per-pillar 50+ leads/6mo + top-10/90d, cluster 15+ MQLs/mo at $50-100 CAC, elasticity 2x, time-to-conversion 30d / 5-10d-faster-than-paid, pipeline 15-20% ACV-weighted, payback ROI 6-9mo / break-even month 4-5 — all removed; elasticity and faster-than-paid claims now demand a within-your-own-data control, ownership seams drawn to `analytics-performance-analyst`/`sales-pipeline-analyst`/`seo-content-optimizer`; the `Publish/Hold/Cut` gate and the ceiling-not-quota pillar-cluster sizing left intact; both dual copies synced, lint 2/2, old strings grepped to zero). **Content sweep complete.** The same pattern now continues in the **social** discipline — see the dedicated item below.
- [ ] Look at `feedback-collector` in [taizen-ai/taizen-claude-plugins](https://github.com/taizen-ai/taizen-claude-plugins) (8★, MIT, pushed 2026-05-25) on a future run. Surfaced 2026-08-24: the collection's other 24 skills map onto agents we hold at greater depth and it has **zero** win/loss surface, but a structured customer-feedback-intake discipline grazes `analytics-customer-insights-researcher` and `growth-customer-marketing-lead` and may or may not be owned. Grep-verify before treating it as a gap.
- [x] **`ops-financial-tracker` enhancement lead — renewal leverage as a named discipline.** — **done 2026-08-29.** Shipped a **"Conducting the Renewal: Leverage Before the Ask"** section on both dual-located copies: the six leverage angles (usage vs. contracted capacity; vendor fiscal-quarter timing, framed as timing not a quotable figure; a credible *named* alternative with switching cost subtracted; tradeable commitment without locking into shelfware; bundle/package seams; the tenure penalty) plus the honesty-is-the-leverage and carry-no-borrowed-thresholds disciplines. Gap verified by read first; the seam was decisive — `analytics-martech-stack-strategist` (its Rules 4 & 10) explicitly hands "the negotiation itself" here and deliberately left that half unwritten, so the hand-off pointed at an empty method. **None of the source's numbers carried** (70% capacity, 15-25% multi-year, 10-20% quarter-end, 10-25% general SaaS — all unsourced), matching the stack strategist's stance. Both copies diff-identical, lint 2/2. Surfaced 2026-08-26 while reading [modest-curator478/claude-skills](https://github.com/modest-curator478/claude-skills) `revops/martech-contract-auditor` (MIT). The agent already owns vendor contracts, renewal dates, the scorecard and negotiation, but names no *method* for the renewal itself — the recurring *control named, instrument absent* shape. The portable ideas are the six leverage angles (usage against contracted capacity; the vendor's fiscal-quarter timing; credible named alternatives; multi-year commitment; bundle and package gaps; long-customer pricing disadvantage) and the honesty posture that naming a real alternative beats bluffing. **Do not carry any of its numbers** — 70% capacity, 15-25% multi-year, 10-20% quarter-end, 10-25% general SaaS are unsourced. Check first against the agent's existing Rule 3/4 and the new `analytics-martech-stack-strategist` boundary, which already hands it the requirement and the switching-cost figure.
- [x] **`email-automation-engineer` enhancement lead — the seed-send checklist row that names a control with no method.** Surfaced 2026-08-26. The agent's pre-flight item 7 reads "Seed send reviewed in Gmail, Outlook, and one mobile client: placement, dark mode, image-blocked fallback" — a one-line control with no grading scheme, and `Litmus`/`Email on Acid`/`preheader` grep to one hit or zero repo-wide. [justinwilliames/orbit-for-claude](https://github.com/justinwilliames/orbit-for-claude) (MIT) ships `email-render-qa`, `email-production-qa` and `liquid-branch-coverage` as separate disciplines; the vendor-neutral parts (render matrix, every conditional branch and fallback path proof-rendered, accessibility and alt-text on a blocked-image render) are worth weighing. Grep-verify against `ops-quality-assurance`'s launch checklist and `design-content-visual-designer` before acting — the severity half may already be covered. — **done 2026-08-29.** Grep confirmed the split the note predicted: the *severity/accessibility half* (WCAG, alt-text, contrast, cross-client render, dark-mode variants) is already owned by `ops-quality-assurance` (Technical QA + Accessibility reports) and `design-content-visual-designer`, so it was **not** duplicated. The genuinely unowned ground is automation-specific: `liquid-branch-coverage`'s core idea — a personalized email has a full state space and the untested arms are where it breaks — has no counterpart here, and this is the one agent that *builds* the branches. Shipped a **"The Email You Seed Is One Render of Many"** section (both dual copies): enumerate the branch/merge-field state space and proof-render every arm + fallback (not just the seed profile's path), record a count not a vibe, plus a send-blocking Gmail clip-line check (~102 KB HTML source, framed as ESP-documented not Google-published) and an explicit seam to the two accessibility/QA owners. Item 7 amended to point to the method. Ideas-only, credited to orbit-for-claude (MIT); lint 2/2, copies diff-identical.
- [x] **`pmm-competitive-intelligence` enhancement lead — a competitor's martech stack from public signals.** Surfaced 2026-08-26 via [almoretti/martech-ai-skills-and-tools](https://github.com/almoretti/martech-ai-skills-and-tools) `martech-teardown` (MIT; the LICENSE file is MIT, the GitHub API reports NOASSERTION because of vendored Apache-2.0 CLIs). The *discipline* is real and unowned here — inferring a competitor's CMS/CDP/CRM/ESP/pixel layer from public pages, DNS/SPF/DKIM, sitemaps and the live tag container, with every claim tagged Confirmed / Inferred / Assumed and "zero regex hits" never read as "no tracking". The skill itself is Playwright-and-script shaped (the executable direction this repo declines), so ideas only, and the evidence-labelling half is our own four-state discipline already. Weigh against the agent's public-sources-only boundary before acting. — **done 2026-08-29.** Gap grep-verified absent first. Shipped **Critical Rule 11** + a **"Reading a Competitor's Stack from Public Signals"** section (signal→layer→confidence table; four-state Confirmed/Inferred/Assumed labelling with Unknown held open; clean-scan-is-Unknown rule for server-side/CAPI/CDP-proxy/consent-gated invisibility; public-and-authorized-only boundary deferring CFAA/ToS to `ops-legal-compliance`; capability-not-performance framing; explicit seam with `analytics-martech-stack-strategist`), plus a **Competitor Tech-Stack Profile** deliverable and a **Stack-Inference Provenance** metric. Ideas-only, no text reused, **no numbers asserted**. Both dual copies diff-identical, lint 2/2.
- [ ] **Licence re-check on a future ops run:** [NHinternesch/tech-stack-audit](https://github.com/NHinternesch/tech-stack-audit) (0★, pushed 2026-08-10) executes martech/website stack audits on public URLs — directly on-thesis for `analytics-martech-stack-strategist` — and **asserts no licence** (GitHub licence API: NONE, checked 2026-08-26), so it was left unread. Read only if it adopts a permissive licence.
- [ ] **Watch:** [NEON-Rutger/B2B-revops-skills](https://github.com/NEON-Rutger/B2B-revops-skills) (45★, MIT, 38 skills, pushed 2026-08-24). Mined once on 2026-08-26 for the martech ADD. **Narrowed 2026-08-28** — the presales ADD run grep-verified this queue as the item asked and closed most of it: `lead-routing` and `revops-handoffs` are `analytics-marketing-ops-architect` **sections at most, not roles** (it already owns the MQL definition, the fit-vs-engagement AND-gate, the routing quadrant table, every-rejected-MQL-needs-a-named-destination and the handoff SLA metric); `deal-desk-operations` resolves to `pmm-pricing-packaging-strategist` (approval matrix) plus `ops-financial-tracker`; `closed-lost-revival` is already claimed by `abm-account-based-strategist` and `sales-pipeline-analyst`. **Narrowed again 2026-08-29** (PMM/GTM scout run): `data-enrichment` is **closed as a role** — tested as this run's ADD candidate alongside [keinsaasforever/gtm-pipeline-skills](https://github.com/keinsaasforever/gtm-pipeline-skills) (69★, MIT, an entire repo devoted to the prospect-data layer) and killed by grep: `sales-outbound-strategist` already carries a *Targeting and List Development Strategy* deliverable with data-sourcing by category, list hygiene and validation, duplicate detection, do-not-contact management and periodic re-validation. It survives only as a possible **section** on that agent, not a persona. **Still unread:** `operating-cadence-designer`. Original note — the unread remainder that greps as adjacent-but-unowned here: `lead-routing` (routing, round-robin, territory and speed-to-lead grep to passing mentions only — one glossary line in `email-automation-engineer`, one phase-2 playbook bullet, one "route now" cell in the ops architect's quadrant table, and no owner), `revops-handoffs`, `operating-cadence-designer` and `data-enrichment`. Grep-verify each before opening; `lead-routing` may be an `analytics-marketing-ops-architect` section rather than a role, since that agent's Core Mission already claims the marketing/sales handoff.

- [x] **`sales-discovery-coach` enhancement lead — per-call coaching has no method here.** Surfaced 2026-08-28 while surveying [matteotitta/genesys-skills](https://github.com/matteotitta/genesys-skills) `sales-enablement/call-coaching` (MIT). `call coaching`, `role play` and `certification` grep to **zero** in `sales/`, yet the Discovery Coach's Core Mission already claims "discovery coaching programs and role-play exercises" — the *control named, instrument absent* pattern again. The source's ideas worth learning: a call-type taxonomy that **selects** the qualification framework rather than applying one blindly, per-element verdicts (covered / partial / missing) each citing a verbatim quote with speaker attribution, "coach the rep, judge the call" (buyer responses are evidence of rep behaviour, not the thing graded), lowering confidence instead of inventing evidence, and one focus for the next call rather than a list. Dismissed as an ADD this run because it is a method inside an existing role, not a role. — **done 2026-08-28.** Added a **Single-Call Discovery Scorecard** (grades one recorded/transcribed call, not the deal — distinct from the existing opportunity-grading Qualification Scorecard) plus a new **Critical Rule 9 "Coach the Rep, Not the Buyer"** to both dual-located copies. Grep-verified the gap first: the call-type-taxonomy idea was already present (Discovery Methodology Taxonomy), and the vague `Discovery Call Review Process` now routes to the scorecard. `role play`/`certification` were non-zero in `sales/` but all in unrelated agents (product security certs; role-play only in this file's own Core Mission/deliverable), confirming the *instrument-absent* diagnosis. Lint 2/2, copies diff-identical, no count change (74/19).
- [ ] **SCOPE QUESTION — is a sales-enablement *program* persona (new-hire ramp, certification, reinforcement cadence, sales kickoff) in scope?** Evaluated as the second ADD candidate on 2026-08-28 and **rejected for now**, with the analysis recorded so it is not re-derived. The gap is partly real — `enablement program`, `onboarding curriculum`, `sales kickoff` and `ramp` (in the seller sense) grep to zero — but three of its four pieces already have owners: `pmm-launch-manager` holds launch training and a competency bar, `sales-discovery-coach` holds coaching and role-play, `sales-enablement-content-creator` holds the assets. And the open-source demand evidence is thin: one unlicensed repo ([tangelo-ai/sales-onboarding-program](https://github.com/tangelo-ai/sales-onboarding-program)) plus vendor API wrappers, against five independent arrivals for presales. Re-open only if two or more permissively-licensed collections ship it as its own role.
- [x] **Anti-fabrication sweep across the social discipline — DONE 2026-08-28 (all four flagged social personas cleaned; sweep complete).** Surfaced 2026-08-27 by the scout ENHANCE on `social-influencer-partnerships`, which removed three fabricated benchmarks (`15%+` engagement gate, `5-10x` value multiplier, the `1% engaged` / `30% engaged` Identity contrast) but left the rest deliberately untouched under the one-change rule. **`social-influencer-partnerships` is now fully de-fabricated — done 2026-08-27** (its Success Metrics scorecard, the Partnership Types fee bands, and the dashboard-deliverable ROI target all cleared; both dual copies diff-identical, lint 2/2, retired strings grep to zero; see [RUN_LOG.md](RUN_LOG.md)). **`social-linkedin-strategist` de-fabricated — done 2026-08-28** (Success Metrics scorecard rewritten to the sweep pattern — follower growth `15-30% YoY` + `5K→7.5K`/`25K→40K+` targets, engagement `2-4%`/`4-8%`, CTR `1.5-3%`, MQL `2-5x`/`15-30%`, advocacy reach `5-15x`/`40-60%`, sales-cycle `20-35%`/`15-25%` all removed; the sales-cycle/win-rate claim now demands a control with self-selection named; the Core-Mission `10-50x` multiplier and the `8-10`/`5` competitor counts retired; the two 2026-08-07 first-hour metrics left intact; seams to `analytics-marketing-ops-architect` + `sales-pipeline-analyst`; both dual copies diff-identical, lint 2/2, retired strings grep to zero — see [RUN_LOG.md](RUN_LOG.md)). **`social-twitter-strategist` de-fabricated — done 2026-08-28** (Success Metrics scorecard rewritten to the sweep pattern — follower growth `3-8%`/`50-150% YoY`/`10%/month`, engagement `2-5%`/`8-15%+`, tweet-consistency `20-30%`/`3x+`, thread `4-8%`/`15-30%` hook-impression share, real-time `60%`/`2x+`, CTR `0.5-1.5%`, community `20-50+` engaged followers, pipeline `5-15%` of SQLs, and visibility `15-30%` quote-tweet share all removed; the pipeline "sourced from X" claim now flagged as attribution not a given and any "converts better" claim demands a like-for-like control; the deliverable `8-10` competitor count and the `(2-4x daily for optimal reach)` false optimum retired; the Test-Discipline and Crisis-Response fidelity checks and all effort/cadence commitments left intact; seams to `analytics-marketing-ops-architect`; both dual copies diff-identical, lint 2/2, retired strings grep to zero — see [RUN_LOG.md](RUN_LOG.md)). **`social-youtube-producer` de-fabricated — done 2026-08-28, closing this sweep** (the whole Success Metrics scorecard rewritten to the sweep pattern — View Growth `10-20%`/`3-8%` monthly + `5,000-15,000` views by month 12, CTR `0.8-2.5%`/`3-5%`, Watch-Time `60-75%`/`75%+`, View Duration `2-4 min`/`1.5min` floor, Subscriber `2-5%` + `30-50%` algo share, Traffic `10-25%`/`15%+`, SEO `30-50%`/`10-20%` top-3, Engagement `2-5%`/`1%` floor, Lead `2-3x` LTV/`5-10%` SQL share, Consistency `50%+`, Repurposing `5+` assets all removed; the LTV/"converts better" claim now demands a like-for-like control; **a factually wrong platform claim corrected** — the YouTube Partner Program bar was stated as `10K watch hours` and is actually **4,000 valid public watch hours + 1,000 subscribers in 12 months** (or 1,000 subs + 10M Shorts views/90d), now cited to [YouTube Help](https://support.google.com/youtube/answer/72851); also cleared the deliverable `8-10` competitor count, the `(2-3x weekly for serious channel growth)` false optimum, the two `0.8-2%`/`5-15%` dashboard targets, and Rule 1's `60%+`/Rule 3's `2-3%` in-rule figures; illustrative Identity example left intact; seam to `analytics-marketing-ops-architect`; both dual copies diff-identical, lint 2/2 at 2372 words, retired strings grep to zero — see [RUN_LOG.md](RUN_LOG.md)). **Social sweep complete — all four flagged personas (`social-influencer-partnerships`, `social-linkedin-strategist`, `social-twitter-strategist`, `social-youtube-producer`) are de-fabricated.**
- [ ] **`social-linkedin-strategist` enhancement lead — the largest single-vendor LinkedIn organic collection is unread.** Surfaced 2026-08-27. [TaplioOfficial/taplio-linkedin-claude-skills](https://github.com/TaplioOfficial/taplio-linkedin-claude-skills) (5★, MIT, 17 skills, pushed 2026-07-15) is written by the team behind a LinkedIn-growth tool and is the fullest LinkedIn organic surface surveyed here to date; left unread this run under the one-change rule. Pairs naturally with the anti-fabrication item above, since the same agent is the target of both. Grep-verify before opening — much of a creator-growth collection is personal-brand ghostwriting, which `content-thought-leadership-ghostwriter` already owns.
- [x] **Decide whether corporate gifting / direct mail is an `abm-account-based-strategist` section or an `events-field-marketing-strategist` one.** Surfaced 2026-08-29 (PMM/GTM scout run) while reading [TheCraigHewitt/sales-skills](https://github.com/TheCraigHewitt/sales-skills) (21★, MIT), whose `direct-mail` skill has no counterpart here: `direct mail` and `cold call` grep to **zero** repo-wide and `gift` appears only in `pmm-customer-advocacy`'s incentive rules. Judged a **section, not a persona** — a physical touch inside an account play. The open question is which of the two agents owns it, and whether the gifting-as-incentive boundary already drawn in `pmm-customer-advocacy` (and `social-influencer-partnerships`' material-connection rule) needs to be restated where it lands. Low risk; do not add an agent for it. — **done 2026-08-30.** Settled on **ABM** (the touch lives inside an account play; the strategist owns its *placement in the orchestration contract*). Shipped as **Rule 11** on both dual-located copies of `abm/abm-account-based-strategist.md`, drawing three seams rather than re-deriving them: consent/suppression via the existing **Rule 8** (which already named "the direct-mail send"); the anti-bribery gift **ceiling** owned by `events-field-marketing-strategist` Rule 7 (5 CFR 2635 / Sunshine Act / FCPA / UK Bribery Act) plus the recipient's-own-policy constraint from `pmm-customer-advocacy`, both routed to `ops-legal-compliance`; and the **incentive bright line** restated where it lands (a gift buys attention, never a conditioned action). Verified in agent content that gifting-as-inducement had no owner before. Lint 2/2, copies diff-identical.
- [ ] **Watch:** [cogni-work/insight-wave](https://github.com/cogni-work/insight-wave) (12★, Apache-2.0, pushed 2026-08-29) — `cogni-marketing` (11 skills) and `cogni-sales` (`why-change`). Surveyed 2026-08-29 and dismissed as already-owned: the marketing skills map onto existing agents, and `why-change` (status-quo bias, cost of inaction) greps to `sales-deal-strategist` and `sales-enablement-content-creator`. It is the most actively-pushed on-thesis collection found this run and its `cogni-sales` plugin holds a single skill, so it looks early. Re-check for new sales skills on a future PMM/sales/GTM run.
- [x] **`pmm-international-gtm-strategist` follow-up — the localization ladder's rung-1 trigger names no method — DONE 2026-08-30.** Closed exactly where the item predicted: the instrument shipped on **`seo-local-and-international`**, not on the GTM agent. Rung 1's trigger now has a named read — Search Console segmented by **query, page and country together**, hunting three absences (in-language queries already returning your default-language page; impressions at respectable positions from a country with collapsed CTR; sessions from that market entering on the default locale without depth) — reported as dated observations, with the go/no-go handed back to `pmm-international-gtm-strategist`. No invented rubric was used; the read is built from what Search Console actually exposes.

- [ ] **Read the remaining nine skills in [ScrapeCreators/social-media-research-skills](https://github.com/ScrapeCreators/social-media-research-skills)** (1,785★, MIT, pushed 2026-08-26) on a future **social** run. Mined once on 2026-08-27 for the influencer-vetting ENHANCE (`outlier-post-finder`, `influencer-prospecting`, `creator-profile-teardown`); `ad-library-teardown` was judged **have** (`pmm-competitive-intelligence` names the LinkedIn and Meta Ad Libraries). Unread and worth grep-verifying: `comment-mining`, `social-listening-brief`, `audience-research`, `transcript-intelligence`, `trend-discovery`, `product-demand-research`, `content-repurposing`. `social listening` greps thin here — `social-community-builder` and `pmm-competitive-intelligence` both graze it and neither owns it as a method. Note the whole collection is a paid-API data layer, so ideas only.

- [x] **SEO-discipline anti-fabrication sweep. — done 2026-09-03 (final persona; sweep complete).** Surfaced 2026-08-30 by the scout ENHANCE on `seo-local-and-international`, which added a section telling the reader to measure their own market and then left, under the one-change rule, a Success Metrics block directly below it asserting unsourced round numbers as fact: *"30-50% monthly organic traffic increase per new market within 12 months"*, *"40%+ of target keywords ranked in top 3 per market within 12 months"*, *"native-level content quality review scores (80%+)"*, *"Capture 20-30% share of voice … by month 12"*, *"Drive 100+ qualified leads/demos monthly … within 18 months"*, *"95%+ consistency across local citations"* — plus a *"minimum 200-300 keywords per market"* quota in the Market-Specific Keyword Research deliverable. Run it as its own job the way the content sweep (closed 2026-08-27) and the social sweep (closed 2026-08-28) were run — which is to say **one persona per run**, not all at once. **STARTED 2026-08-31:** `seo-local-and-international` de-fabricated (the surfacing agent, done first, mirroring how the influencer-partnerships agent led the social sweep) — all seven flagged strings above retired, every bullet rewritten to the baseline-from-your-own-history / control-for-causal pattern with no fixed figure, the *native-level score* bullet turned into the review-record process check (target 100%), international pipeline handed to `analytics-performance-analyst`; the *Hreflang set validity* 100% binary kept; both copies diff-identical, lint 2/2, retired strings grep to zero. **CONTINUED 2026-08-31 (fourth run):** `seo-content-optimizer` de-fabricated — the seven opening Success-Metric bullets (*+2-3 position within 30 days*, *15-20% featured snippet within 90 days*, *25-35% impressions within 60 days*, *20-30% CTR within 45 days*, *40%+ crawl-depth increase*, *15-20% pipeline within 6 months*, *5-10 positions per hour*) all retired to the baseline-from-your-own-history / require-a-control pattern with no figure asserted; the six already-disciplined process-checks below (lines ~195-200) left untouched as flagged; pipeline handed to `analytics-performance-analyst`. **Ledger correction:** the *40%+ crawl-depth* bullet was fabricated and in the block but had NOT been enumerated in this item's flagged-string list above for this persona — caught and retired anyway. Both copies diff-identical, lint 2/2, retired strings grep to zero. **CONTINUED 2026-09-01 (fifth run):** `seo-keyword-researcher` de-fabricated — the seven fabricated Success-Metric round numbers (*40-60% traffic within 6 months*, *30%+ top-3 within 12 months*, *50%+ qualified traffic within 6 months*, *60%+ long-tail by month 6*, *60%+ difficulty win rate within 12 months*, *20%+ snippet within 6 months*, *35%+ SOV within 12 months*) all retired to the baseline-from-your-own-portfolio / require-a-control pattern with no figure asserted; buyer-qualified-traffic attribution handed to `analytics-performance-analyst`; the already-disciplined AI-query-landscape-coverage bullet kept; a lead-in paragraph added flagging that third-party volume/difficulty scores are directional inputs, not scored outcomes. Both copies diff-identical, lint 2/2, retired strings grep to zero. **CONTINUED 2026-09-02 (sixth sweep persona):** `seo-link-building-strategist` de-fabricated — all seven flagged round numbers above (*10-15 links/month from DA40+*, *25-40% referral within 12 months*, *5-10 point DA increase*, *15-20% correlation*, *3-5 publications 500+ clicks*, *20-30% pitch conversion*, *20-30+ media mentions annually*) retired to the baseline-from-your-own-history / require-a-control pattern with no figure asserted; DA/DR flagged as third-party estimates, not a Google signal; referral pipeline and ranking-impact attribution handed to `analytics-performance-analyst` with the ranking claim required to be a like-for-like control read; the four already-honest bullets (shortlist-presence, paid-hygiene binary, >15%-per-source velocity diversity, disavow discipline) kept untouched. Both copies diff-identical, lint 2/2, retired strings grep to zero. **CONTINUED 2026-09-03 (seventh sweep persona):** `seo-ai-search-optimizer` de-fabricated — all six flagged fabricated bullets (*2x citation within 12 months*, *40%+ within 6 months*, *50%+ within 12 months*, *5-10% traffic attribution*, *25-35% content citation rate*, *30-60 days before mainstream*) retired to the baseline-from-your-own-tracked-question-set / read-as-a-trend / require-a-control pattern with no figure asserted; a lead-in added naming the AI-answer-specific measurement problem (no reliable denominator of total possible citations; AI-referred traffic is a floor not a total); AI-referred-traffic attribution defers GA4 channel-group construction to `analytics-marketing-ops-architect` per the file's existing *Measuring Arrival* seam; the three already-disciplined bullets (entity fact consistency, brand-question recognition, topical authority) kept verbatim. Both copies diff-identical, lint 2/2, retired strings grep to zero. **Remaining, one per future SEO run** (grep-verified): `seo-technical-auditor` (*reduce crawl waste 40%+ from 1.8x to under 1.2x*, *30%+ SERP features within 90 days*, *20-30% visibility*, *reduce TTFB by 50%* — **keep** the CWV field thresholds at line 56, cited to web.dev, and the process-check bullets). `seo-programmatic-strategist` is **already clean** — its whole Success Metrics block is process/discipline-framed, no fabricated business outcome, nothing to do. **COMPLETED 2026-09-03 (final SEO sweep persona):** `seo-technical-auditor` de-fabricated — the four flagged fabricated bullets (*reduce crawl waste 40%+ from 1.8x to under 1.2x*, *30%+ SERP features within 90 days*, *20-30% search-visibility increase*, *reduce TTFB by 50%*) retired to the track-against-your-own-baseline / read-as-a-trend / require-a-control pattern with no figure asserted; the crawl-ratio bullet reads its improvement against the intended-exclusion map so correctly-excluded URLs aren't miscounted as waste progress, the visibility bullet flags the tool aggregate as not a Google metric and gates causal credit behind a control, TTFB re-based to 75th-percentile field data as an LCP input. The four process-check bullets (index disposition, migration signal retention, sitewide-drop class-before-fix, audit shelf life) and — as this item instructed — the **web.dev-cited CWV field thresholds at line 56** (LCP ≤2.5s / INP ≤200ms / CLS ≤0.1, 75th-pct mobile) were kept untouched. Both copies diff-identical, lint 2/2, retired strings grep to zero. **With this the SEO-discipline sweep is complete** — all six non-clean personas de-fabricated (`seo-local-and-international`, `seo-content-optimizer`, `seo-keyword-researcher`, `seo-link-building-strategist`, `seo-ai-search-optimizer`, `seo-technical-auditor`), plus `seo-programmatic-strategist` verified clean.

- [ ] **Is Rule 5 of `seo-local-and-international` right for B2B SaaS?** Surfaced 2026-08-30. It reads *"Require local citation building (Google Business Profile, regional directories) for each market; local signals boost regional rankings even for international B2B SaaS"* — a blanket requirement, where Google Business Profile's own eligibility rests on a physical location or in-person contact with customers, which most B2B SaaS entering a new market has neither of. Likely needs a conditional (the profile when there is a real office or in-market field presence; regional directories and in-market link/citation work otherwise) rather than deletion. Check Google's current Business Profile eligibility guidance as a primary source before touching it; do not rewrite from memory.

- [x] **`content-blog-strategist` / multilingual publishing — probably not a gap, verify once.** Surfaced 2026-08-30 while dismissing [AgriciDaniel/claude-blog](https://github.com/AgriciDaniel/claude-blog) (1,975★, MIT, pushed 2026-08-28), whose `blog-multilingual` / `blog-translate` / `blog-localize` tree is the fullest multilingual *publishing pipeline* surveyed here. It was judged CMS mechanics (translate-while-preserving-frontmatter, CMS-ready language maps, per-locale schema emission) rather than a marketing discipline, with the strategy half already owned by `content-blog-strategist` and `seo-local-and-international`. Worth one grep-verified confirmation on a future content run that *translated blog content specifically* — which posts get translated, in what order, and who retires a stale one — has a real owner and not just an implied one. **CLOSED 2026-09-03:** re-read on-thesis during the content/SEO scout run and confirmed exactly as predicted — `claude-blog` is a generation pipeline whose *strategy* half is already owned by `content-blog-strategist` and `seo-local-and-international`, and whose brief specification is search-shaped and duplicates `seo-keyword-researcher`. One idea was carried from it into the new `content-operations-manager` (a written delivery contract with explicit gates between briefing, drafting and publication); nothing else was adopted.
- [x] **Content-discipline anti-fabrication sweep — done 2026-09-03 (third run).** Surfaced 2026-09-03 by the scout ADD of `content-operations-manager`, whose own Success Metrics were written benchmark-free on purpose. On audit, five of the six siblings (`content-case-study-producer`, `content-copywriter`, `content-newsletter-curator`, `content-video-script-writer`, `content-whitepaper-architect`) were **already** de-fabricated and carry the baseline-from-your-own-history discipline; the lone outlier, **`content-thought-leadership-ghostwriter`**, was de-fabricated this run — nine fabricated round numbers retired (incl. a *$50K–200K* AVE bullet routed to the repo-wide ban in `comms-pr-strategist` Rule 9 / Barcelona Principles 4.0, and a *top-5 results* rank claim the executive doesn't control), attribution handed to `comms-pr-strategist` / `comms-analyst-relations-manager` / `sales-pipeline-analyst` / `analytics-performance-analyst`, a stray editorial-calendar restatement of the same figure caught, four already-honest bullets kept; both copies diff-identical, lint 2/2, retired strings grep to zero. **Residual (deferred, not a metrics bullet):** Rule 1's rhetorical *"outperform … by 5–10x in engagement"* — a Critical-Rule intensity claim left out of scope, matching how prior sweeps left illustrative in-rule figures alone; handle only if a future run does a rules-level pass. `content-blog-strategist` was already de-fabricated before the sweep opened. Same one-persona-per-run pattern as the closed social content sweep and the live sales/SEO/analytics sweeps.
- [ ] **[yaojingang/GEOHub](https://github.com/yaojingang/GEOHub) (146★, **AGPL-3.0**, pushed 2026-08-31) — ideas-only, unread.** Surfaced 2026-09-03. The first evidence-bounded GEO/AI-search collection seen in this ledger, and the only AGPL source of interest so far: copyleft means **ideas only, no derivative of its expression**, so read it that way or not at all. On-thesis for `seo-ai-search-optimizer`, which already holds citability scoring (07-26), crawler-access auditing (07-30) and a measurement backbone — so grep-verify a real gap before carrying anything. Pairs with three unread MIT SEO suites from the same run ([TheSmokeDev/geo-skills](https://github.com/TheSmokeDev/geo-skills), [seoskillsai/seo-skills-ai](https://github.com/seoskillsai/seo-skills-ai), [kpab/seo-mastery-agent-skills](https://github.com/kpab/seo-mastery-agent-skills)), all technical-audit-and-schema shaped: only adopt from them if one offers a *judgement* rather than a checklist.
- [ ] **Analytics-discipline anti-fabrication sweep.** Surfaced 2026-08-31 by the scout ENHANCE on `analytics-customer-insights-researcher`, which rewrote Rules 3/5 and one Success Metric off its interview/response quotas and then left the rest of that block untouched under the one-change rule. Still asserting unsourced round numbers as fact in that agent: *"80%+ of research findings directly influence product decisions"*, *"85%+ of sales and product team agree personas accurately represent their customers"*, *"70%+ of major recommendations … are implemented"*, and *"findings translated to insights within 3-4 weeks"*. Its five analytics siblings are unaudited — `analytics-data-storyteller`, `analytics-conversion-rate-optimizer`, `ops-reporting-specialist` and the project-management personas are the likeliest carriers, while `analytics-performance-analyst`, `analytics-marketing-ops-architect` and `analytics-martech-stack-strategist` have all been worked recently and are probably clean. Run it **one persona per run**, the way the content (closed 2026-08-27), social (closed 2026-08-28) and SEO (started 2026-08-31) sweeps were run. **Started 2026-08-31:** `analytics-customer-insights-researcher` cleaned — all four remaining fabricated round numbers (`80%+`, `85%+`, `70%+`, `3-4 weeks`) rewritten into tracked, measured disciplines (utilization recorded as named decisions; persona accuracy checked by logging where sales/product disagree; recommendation adoption tracked as per-item status incl. declines/disappearances; velocity reported as a trend, not a calendar target). Both dual-located copies updated, diff-identical, lint 2/2. Siblings still unaudited — continue with `analytics-data-storyteller` / `analytics-conversion-rate-optimizer` / `ops-reporting-specialist` next. **CONTINUED 2026-09-03 (second persona):** `analytics-data-storyteller` de-fabricated — the whole ten-bullet Success Metrics block (`80%+`/`85%+` utilization+satisfaction, `90%+` comprehension, `70%+ within 2 weeks` decision velocity, `within 5/10 business days` delivery, `99%+` accuracy, `95%` visualization takeaway, `80%+` post-mortem learning application, `90%` stakeholder trust, `85%+` QBR satisfaction driving `60%+` renewal) retired to the baseline-from-your-own-history / named-decision / read-as-a-trend pattern with no figure asserted; definitional consistency and post-mortem coverage kept as zero-drift/full-coverage aspirations; the QBR renewal/expansion attribution handed to `analytics-performance-analyst` as a many-driver like-for-like read. Both copies diff-identical, lint 2/2, retired strings grep to zero. Remaining, one per future analytics run: `analytics-conversion-rate-optimizer` (its Test-Feasibility/Experiment-Trust records already clean; Success-Metrics block still carries round numbers), `ops-reporting-specialist` (`client-ops/`). **CONTINUED 2026-09-04 (third persona):** `analytics-conversion-rate-optimizer` de-fabricated — seven of the eleven Success-Metric bullets (`20-35%` overall lift, `15-30%` page-specific, `30-50%` form completion + `typically 25-35%` benchmark, `5-10% overall` from top-three wins, `typically 2-5%`/`25-40% for bottom` industry benchmarks, `improves 20-30%` satisfaction, `8-12% increase` pipeline multiplier) retired to the baseline-from-your-own-history / read-as-a-trend pattern with no figure asserted; each metric and diagnostic kept, lift now weighed next to the confidence the test earned it (tying back to the file's own SRM/stopping-rule trust gate) and revenue impact named as a multi-driver like-for-like read, not a fixed multiplier, per the `analytics-marketing-ops-architect` seam. Four bullets kept verbatim — Lead Quality Consistency, Test Velocity (its `2-4 experiments/month` work-cadence and `60-70%-win-rate-is-a-flag` framing are correct — cadence-of-activity is not an outcome claim), Experimentation Documentation (100%-documented = zero-drift aspiration), Feasibility Discipline (already process-framed). Both copies diff-identical, lint 2/2, all nine retired strings grep to zero. Remaining, one per future analytics run: `ops-reporting-specialist` (`client-ops/`). **CONTINUED 2026-09-05 (fourth persona):** `ops-reporting-specialist` de-fabricated — seven of the ten Success-Metric bullets (`80%+`/`90%+` dashboard access+usefulness, `100% on time` + `within 5 business days` delivery, `99%+` accuracy + `over 5%` discrepancy, `90%+` comprehension, `70%+` insight generation + `60%+` actions implemented, `80% reduction` in reporting time, `85%+` satisfaction + `NPS of 50+`) retired to the baseline-from-your-own-history / named-decision / read-as-a-trend pattern with no figure asserted; adoption read as which dashboards get pulled into decisions unprompted, timeliness against your committed cadence with slippage causes distinguished, accuracy as source-system reconciliation (Rule 8) against your own materiality threshold, comprehension as falling clarification requests, insight/action as per-recommendation status with the dropped ones counted, efficiency against your pre-automation baseline, satisfaction as behavioral signals not a self-scored rating/NPS. The three already-honest bullets (Strategic Alignment, Comparative Performance, Scalability) kept verbatim. Both copies diff-identical, lint 2/2, all retired strings grep to zero. **Correction to this item's own "probably clean" note:** a re-scan of the three siblings assumed clean found otherwise — `analytics-martech-stack-strategist` is clean, but `analytics-performance-analyst` (`80%+`/`85%`/`99%+`/`90%+`/`70%+` in its dashboard/accuracy/report-quality bullets, `20-30%` ROI improvement, `85%+` pipeline attribution) and `analytics-marketing-ops-architect` (`95%+`/`99%+`/`70%+`/`30%+`/`99.9%+`/`80%+` in its data-quality/lead-scoring/uptime/attribution bullets) both still carry round numbers in the bullets their recent targeted edits did not touch. Sweep is **not** complete — continue one per run on `analytics-performance-analyst` and `analytics-marketing-ops-architect` (grep-verify each block first; several of their bullets have already been rewritten into the disciplined `target 100%`-aspiration style and must be left alone). **CONTINUED 2026-09-05 (fifth persona):** `analytics-performance-analyst` de-fabricated — seven of its twelve Success-Metric bullets (`80%+`/`85%+` dashboard access+satisfaction, `99%+` accuracy, `90%+`/`70%+` report recommendations+action-implementation, `20-30%` marketing-ROI improvement, `85%+` pipeline/revenue attributed, monthly-by-`5th-business-day` / `within 10 days` delivery day-counts, `60%+` of tests improving performance) retired to the baseline-from-your-own-history / read-as-a-trend / require-a-control pattern with no figure asserted; adoption read as which dashboards get pulled into decisions unprompted, accuracy as source-of-record reconciliation against your own materiality threshold, report actionability as per-recommendation status with the dropped ones counted, channel-ROI and revenue-attribution causal claims gated behind a like-for-like control with the attribution model routed to `paid-media-attribution-analyst` (already the file's own routing-table owner), delivery against your committed cadence with slippage cause named, and test-impact deferred to the experiment's own controls per `analytics-conversion-rate-optimizer`. The five already-honest bullets kept verbatim — Funnel Performance Transparency, Anomaly Detection Timeliness (its `95%+`-within-declared-range coverage aspiration), Verdict Quality (`100%`-closed aspiration + false-alarm tracking), Segmentation Depth (`5-7`-segment work-scope with its anti-quota discipline), Movement Reconciliation. Both dual copies diff-identical, lint 2/2, all retired strings grep to zero. **Remaining, one per future analytics run:** `analytics-marketing-ops-architect` (grep-verify first; several bullets already in the `target 100%`-aspiration style).
- [x] **Cold-email sending infrastructure — enhancement lead, not a role.** Surfaced 2026-08-31, **done 2026-09-05**. Owner decided: **`email-deliverability-specialist`**, not `sales-outbound-strategist` — the harm the discipline prevents (a cold program burning the domain that carries invoices, password resets and support replies) is that agent's asset, and every mechanism involved already lives in its file; outbound keeps the estate as capacity and economics, and both boundaries are now stated in-file. Shipped as Critical Rule 10 + the *Two Estates, Opposite Defaults* section + a *Cold Sending Estate Architecture & Authentication Baseline* deliverable + two metrics, with the vendor-neutral half of [Growth-Today/claude-skills](https://github.com/Growth-Today/claude-skills) `gt-email-infra` (MIT) and one idea from [chunkydotdev/email-skills](https://github.com/chunkydotdev/email-skills) (MIT); the tool-bound half of both was left behind as planned. See the 2026-09-05 [RUN_LOG](RUN_LOG.md) entry.
- [x] Resolved 2026-08-31: **no hard 1,024-char limit exists.** `claude plugin validate` still 404s on its pinned model in the automated session, so the answer was read straight from the installed CLI's own validator (`@anthropic-ai/claude-code` 1.0.72): the only length rule on a skill/agent `description:` is a **soft warning at "over 5000 characters"** (`whenToUse.length>5000`) — required + a "at least 10 characters" floor are the only other checks, and `.max(1024)` appears **zero** times in the bundle. `marketing-analytics` at 1,148 chars (max of all 19; next is `paid-media-ops` at 934) is well within bounds and needs no trim. Future runs: stop trimming descriptions for a phantom 1,024 cap; only descriptions over ~5,000 chars would even warn.
- [ ] **Social program / account-continuity residue — an enhancement lead, not a role.** Surfaced 2026-09-01 and grep-killed as an ADD candidate. `scheduled post`, `publishing queue`, `offboard`, `social media policy`, `two-person` and `social customer care` all return **zero** repo-wide, which looks like a gap until you check the market: the largest example ([stevenflanagan1/social-ai-team](https://github.com/stevenflanagan1/social-ai-team), 202★, **no licence**) is an orchestrator that routes to nine component skills — which `catalyst-orchestrator` already is — and [seranking-planable/smm-skills](https://github.com/seranking-planable/smm-skills) (MIT) is agency scheduling plumbing around a vendor MCP. Crisis and moderation are also already seamed three ways (`comms-pr-strategist` company-level, `social-community-builder` owned spaces, `social-twitter-strategist` public pile-on). What is genuinely unowned and worth a *section* on a future social run: **account access and continuity** (who holds the credentials, what happens at offboarding, dormant and squatted handles) and **the publishing approval gate as a standing control** rather than something improvised during a pile-on. Place on `social-community-builder` or `social-twitter-strategist`; do not open a persona.
- [ ] **`audience-overlap` — a paid-media enhancement lead.** Surfaced 2026-09-01 from [MadalaVijay/paid-ads-skills](https://github.com/MadalaVijay/paid-ads-skills) (MIT, pushed 2026-09-01), the sharpest small paid collection surveyed — six skills, each a decision rather than a platform wrapper. Five resolve to existing owners. The sixth does not: **audience collision across simultaneously-running paid campaigns** greps to nothing as a named check anywhere in `paid-media/`, and two campaigns bidding against each other for the same people inflates cost with no error surfaced by either platform. Grep-verify first (`audience overlap`, `self-competition`, `bidding against`), then place on `paid-media-social-ads-specialist` or `paid-media-budget-optimizer` — the seam matters because one owns the platform mechanics and the other owns the portfolio.
- [ ] **`read-the-room` and `trend-radar` — a future social run.** Surfaced 2026-09-01 in [scrollmark/social-skills](https://github.com/scrollmark/social-skills) (13★, MIT, pushed 2026-08-28), a social-media vendor's collection dismissed this run because its centre of gravity is consumer creator video production (Remotion compositor, TTS, caption burn-in, CapCut/FCPXML export) — the executable direction this repo has declined repeatedly. Two of its judgement-shaped skills were not read: `read-the-room` (whether the moment is right to post at all) and `trend-radar` (distinguishing a durable shift from a passing format). Both are adjacent to `social-twitter-strategist`'s pile-on playbook and `content-blog-strategist`'s calendar. Grep-verify before treating either as a gap; be skeptical, since "post the trend" is exactly the off-brand consumer instinct the repo has rejected before.
- [ ] **Narrowing note for line 92 (the `modest-curator478` / [matellez/claude-skills](https://github.com/matellez/claude-skills) `leadership/` tree).** Recorded 2026-09-01: the two repos ship the same skill set, so treat them as one source. And `marketing-budget-defense` is now **adjacent to, but not the same as**, the new `pmm-brand-demand-strategist`'s Rule 3 — defending *brand* spend specifically has an owner as of this run; defending the whole function's budget is still the open grade-the-marketing-function SCOPE QUESTION (line 86), which remains a maintainer call.
- [ ] **Grep-verified unowned but judged too narrow for a persona (2026-09-01):** `product hunt` and `app store optimization` both return **zero** repo-wide, and both appear as first-class skills in creator-oriented collections ([poorvith-mp/skills-marketing](https://github.com/poorvith-mp/skills-marketing), MIT). Neither is a role for a B2B SaaS roster on current evidence — Product Hunt is a launch *channel* (`pmm-launch-manager` ground) and ASO applies only to the minority of B2B SaaS with a consumer-store presence. Recorded so a future run does not re-derive the check; reopen only if independent B2B evidence appears.
- [ ] **Forecast-category discipline — a `sales-pipeline-analyst` enhancement lead, not a role.** Surfaced 2026-09-02 while shipping the mutual action plan onto `sales-deal-strategist`. `forecast category`, `commit forecast`, `weighted pipeline`, `sandbagging` and `forecast call` all return **zero** repo-wide. `sales-pipeline-analyst` already owns stage-weighted probability, historical-accuracy challenge, variance tracking and stall alerting, but has no named **commit / best case / pipeline / upside** taxonomy with exit criteria, and no inspection questions behind a commit. Source read this run: [majiayu000/claude-skill-registry](https://github.com/majiayu000/claude-skill-registry) `skills/data/forecast-discipline` (MIT, verified 2026-09-02) — its structure is on-thesis; its numeric targets (95% plan-logged, >90% accuracy, 5% commit-change threshold) must **not** be adopted, they are one author's operating standard. The new *one date, two systems* rule on the Deal Strategist gives this a natural hook: the plan is the forecast's evidence.
- [ ] **Sales-discipline anti-fabrication sweep.** Surfaced 2026-09-02 by the scout ENHANCE on `sales-deal-strategist`, which added a section and two benchmark-free metrics and then left, under the one-change rule, a pre-existing Success Metrics block directly below asserting unsourced round numbers as fact: *"target 60-90 days for mid-market, 120-180 for enterprise"*, *"target 25-40% for qualified pipeline"*, *"target 80%+ within 2-week window"*, *"target 3-5 for mid-market, 5-7 for enterprise"*, *"target 80%+"* champion identification, and an *"above ~10%"* unclassified-loss threshold. The sales discipline is now the largest untouched surface for the sweep that has already run through content, social, analytics and SEO. Same treatment as those: re-base each target against the reader's own historical baseline, keep the metric, drop the invented number. `sales-discovery-coach`, `sales-pipeline-analyst`, `sales-proposal-architect` and `sales-outbound-strategist` need the same check. **STARTED 2026-09-02 (third run):** `sales-deal-strategist` de-fabricated — the surfacing agent cleaned first, mirroring how `seo-local-and-international` and `analytics-customer-insights-researcher` led their own sweeps. All six flagged round numbers above retired to the baseline-from-your-own-history / read-as-a-trend pattern, metric and diagnostic kept in each (cycle length against own segment history + distribution watch; win rate comparable only behind a consistent qualification bar; forecast accuracy as own trend with directional bias watched separately from spread; stakeholder count as a trend with the headcount-padding trap named; champion identification measured *validated not nominated* over the Stage-3-open denominator; unclassified-loss share driven down against its own trend, the honest *100% classified* aspiration kept). No figure introduced. Both copies diff-identical, lint 2/2, all six strings grep to zero. **CONTINUED 2026-09-02 (fourth run):** `sales-discovery-coach` de-fabricated — nine of its ten Success-Metric bullets asserted round numbers (*8-12* questions/call, *30-40% rep / 60-70% buyer* talk ratio, *2-3* problems/call, *60-80%* quantification, *85%+* qualification accuracy, *60-70%* discovery-to-advance, *10-15% YoY* cycle reduction, *35%+* win rate for strong qualification, *15-25%* disqualification) all retired to the baseline-from-your-own-history / read-as-a-trend pattern with no figure asserted; the talk-ratio bullet keeps its durable principle (buyer talks more than rep) but drops the fixed split; the win-rate signal becomes the *gap* between high/low-quality discovery close rates with the qualification bar held constant; advance rate read *against* the disqualification rate. The tenth bullet (Rep Coaching Adoption, already process-framed) kept untouched. Both copies diff-identical, lint 2/2, all ten strings grep to zero, block carries no numeric figure. **CONTINUED 2026-09-02 (fifth run):** `sales-proposal-architect` de-fabricated — eight of its ten Success-Metric bullets (*20-30%* win rate, *14-30 days* velocity, *40-60%* RFP win rate, *<15%* price-cited loss, *70%+* customization, *<5%* compliance breach, *90%+* timeline accuracy / no slippage beyond one week, *80+/100* quality score) all retired to the baseline-from-your-own-history / read-as-a-trend pattern with no figure asserted (win rate held behind a consistent qualification bar and segmented; RFP rate paired with a bid/no-bid discipline; customization watched paired with win rate; compliance breach an honest zero-aspiration process check; timeline accuracy shrinking the promised-vs-actual gap with bias read separately from spread; quality score calibrated across reviewers and checked against whether higher scores actually win). The two already-honest bullets (Executive Summary Utilization, Reference Call Volume) kept verbatim. **Ledger note:** a ninth identical assertion sat *outside* the Success Metrics block — *"target 10-15% higher win rate with proposal"* in the Effectiveness-Metrics deliverable — caught and retired in the same pass; the file's illustrative template figures (Rule 3's specificity example, the placeholder-laden case-metrics line, the worked pricing walkthrough) left alone as reader-fills-own-specifics guidance, outside the sweep's scope. Both copies diff-identical, lint 2/2, all nine strings grep to zero. **Remaining, one per future sales run:** `sales-pipeline-analyst` (entangled with the forecast-category lead, line 146 — grep-verify the seam), `sales-outbound-strategist` (opens/replies already disciplined; meeting-rate, cost-per-meeting, completion and unsubscribe targets are not) — grep-verify each block before touching it.
- [ ] **Read the remaining sales trees in [majiayu000/claude-skill-registry](https://github.com/majiayu000/claude-skill-registry)** (586★, MIT, pushed 2026-09-02) on a future **sales** or **partnerships** run. Mined once on 2026-09-02 for the mutual-action-plan ENHANCE (`skills/data/forecast-discipline`). Unread and on-thesis: `skills/business/deal-review`, `skills/business/enterprise-sales`, `skills/data/founder-sales`, `skills/data/partnership-bd`. One of the most actively-pushed collections in the ledger. Grep-verify before treating anything in it as a gap.
- [ ] **`sales-outbound-strategist`'s *Compliance and Deliverability Playbook* is now the thin half of a seam that has a deep half.** Surfaced 2026-09-05 by the cold-estate ship on `email-deliverability-specialist`, and deliberately left alone under the one-change rule. That agent's four-bullet playbook still reads as though it owns sending infrastructure ("Email authentication: SPF, DKIM, DMARC setup for sender domain") and asserts an unsourced round ramp (*"100 → 500 → 2000 daily over 30 days"*) with no stated basis. Two fixes, one edit: point the playbook at the deliverability specialist as the owner of the estate (keeping outbound's real half — sizing, cost, volume targets), and de-fabricate the ramp the way the anti-fabrication sweep has done elsewhere. Low risk, clearly scoped; do it on a sales or email run.
- [ ] **Should Rule 3 of `email-deliverability-specialist` be re-titled off "IP Warming"?** Surfaced 2026-09-05. The 09-05 ship corrected Core Mission bullet 3 and scoped warming correctly inside the new section, but left **Rule 3** titled *IP Warming Discipline* and the *IP Warming Strategy & Execution* deliverable IP-framed, to keep the change to one seam. On modern B2B SaaS infrastructure — shared ESP pools, Google Workspace and Microsoft 365 mailboxes — most readers never own an IP, so the rule names a control that does not apply to them while the applicable one (domain and mailbox) sits in prose below. Re-title and re-scope both on a future email run; no new source needed, the argument is already in-file.
- [ ] **Watch:** [chunkydotdev/email-skills](https://github.com/chunkydotdev/email-skills) (2★, MIT, pushed 2026-03-31, ~18 skills). Mined once on 2026-09-05 for the warming-scope correction. Its `deliverability/` tree was read; **unread and adjacent**: `inbound/reply-classification`, `inbound/thread-management`, `inbound/inbound-processing`, `compliance/email-security`, `compliance/suppression-lists`, `content/spam-filter-avoidance`. Reply classification and thread management are the one genuinely unowned-looking corner — grep `reply classification`, `auto-reply`, `out of office` and `thread management` before opening, since `sales-outbound-strategist` and `email-automation-engineer` may already hold the routing half. Note the repo has not been pushed since March 2026; check it is still alive before spending a run on it.
- [ ] **Licence re-check, cold-email deliverability:** [harveylegacygtm/email-deliverability-tool](https://github.com/harveylegacygtm/email-deliverability-tool) (0★, pushed 2026-08-17) and [joeoliveimpact/client-deliverability](https://github.com/joeoliveimpact/client-deliverability) (0★, pushed 2026-07-20). Both are directly on-thesis by description (building cold sending infrastructure from scratch; dedicated sending-domain setup and DNS audit) and both **assert no licence** (GitHub licence API, checked 2026-09-05), so both were left **unread** on 2026-09-05 rather than mined. Read only if either adopts a permissive licence. Recorded so a future run does not rediscover them and read them by accident.
